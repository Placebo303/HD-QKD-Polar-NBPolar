# Explicit authorization — Phase 4-P19

I authorize Phase 4-P19 exactly as frozen in
`.workbuddy/queue/NBPOLAR-PHASE4-P19-HOLDOUT-BACKOFF-DIAGNOSTIC/TASK_PACKET.md`.

You may implement the scoped OpenSpec delta, thin diagnostic runner and
injected tests; run the frozen focused/predecessor test set; obtain independent
reviewer-go Pre-EXECUTE review; and, only after PASS, execute the five-arm
diagnostic once on the three registered 1M HOLD blocks. This authorizes one
V25 TRAIN NPZ content open, one 1M pairs-parquet content open and one
scientific attempt, consumed at the first protected content open.

Run exactly the registered base, L1-plus, L2-plus, both-plus and true-L1
control arms with their frozen K values. Do not add or change arms, orders,
increments, blocks, tags, thresholds or models. Do not shuffle, overlap,
resample, pad, pool, fit on HOLD or use the remainder. The oracle control must
remain provenance-isolated and must not be presented as operational.

This gate has no recovery, Wilson, FER, superiority or qualification
threshold. Every recovery pattern is descriptive COMPLETE if integrity holds.
After execution obtain independent reviewer-go Pre-RESULT review and preserve
the five-file evidence root. On any failed review, identity mismatch,
unauthorized access, semantic/resource failure or existing output root, STOP
and return the exact evidence. Do not repair, rerun, retune, clean, commit or
push. Finish with memory triage and return for main-thread acceptance.
