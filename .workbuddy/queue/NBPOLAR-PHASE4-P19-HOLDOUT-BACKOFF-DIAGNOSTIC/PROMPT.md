# Operator prompt — Phase 4-P19

Implement and, only with the companion authorization, execute `TASK_PACKET.md`
exactly. Read `AGENTS.md`, `AGENT_PROJECT_MEMORY.md`, and accepted P16–P18
records first. You are an operator, not the scientific decision owner.

Reuse P18 input/block wiring and accepted operational helpers. The five arms,
K values, order and provenance are frozen. Do not add a grid, interpolation,
adaptive choice, recovery threshold or post-result arm. The true-L1 arm is an
oracle diagnostic control, never a deployable result.

Use independent reviewer-go for both mandatory reviews. Obey one-open,
one-attempt, checkpoint, STOP and no-rerun rules. Do not commit or push. End
with memory triage and return the compact frozen evidence.
