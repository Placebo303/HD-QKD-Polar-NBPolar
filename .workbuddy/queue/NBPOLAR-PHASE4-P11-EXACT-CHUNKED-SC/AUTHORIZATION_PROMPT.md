I authorize the complete Tier-Y task in `.workbuddy/queue/NBPOLAR-PHASE4-P11-EXACT-CHUNKED-SC/TASK_PACKET.md`: add the P11 OpenSpec delta; implement only the exact row-chunked `_minus_block` allocation in `sc.py` with keyword-only default `chunk_rows=512` and `None` golden direct mode; add the frozen focused tests and run the accepted 252-test predecessor suite; freeze the exact gate command/root/schema and obtain independent reviewer-go Pre-EXECUTE PASS; then consume the sole attempt at the first formal `sc_decode` and execute the semantic matrix, paired direct-vs-chunked N=65536 check, and default-chunk N=262144 resource check once with seed 2026091800 under the frozen limits. Obtain independent Pre-RESULT review before return. No artifact/evidence-root/raw/held-out/real/EVAL/tag/protocol access, rerun, tuning, seed/chunk/threshold change, FWHT/APP/SCL, FER/efficiency/key-rate/qualification/promotion claim, old-root modification, commit or push. STOP and preserve evidence on any blocker or failed gate.
