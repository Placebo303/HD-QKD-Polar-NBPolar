# P2-R1 prompt — prepared, not authorized

After explicit authorization, implement only the Bob-axis indexing recovery,
prove it on asymmetric tensors and an independent loop oracle, run all 79
predecessor tests, and obtain independent Pre-EXECUTE PASS. Only then use one
new read attempt on the same artifact and original absent target. Finish with
independent Pre-RESULT review. Never enter SC, P3, DEV/EVAL or raw data.
