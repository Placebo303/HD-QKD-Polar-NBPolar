Implement and execute the complete frozen Tier-Y task in `TASK_PACKET.md` only
after the user supplies its exact authorization. Treat P8 as accepted but
left-boundary censored. Reuse its implementation and semantics through the
smallest explicit delta. Do not redefine the grid, thresholds, streams,
selection, support rule, order, accounting, attempt point, labels, or scope.

Obtain independent reviewer-go Pre-EXECUTE PASS before the first NPZ content
open. That open consumes artifact read 1/1 and scientific attempt 1/1. Execute
the frozen command once. Never rerun, retune, change a seed/grid/threshold, or
repair after consumption. Obtain independent Pre-RESULT review before return.

Return only after completion or a concrete blocker. Report changed files,
tests, reviews, read/attempt/seed accounting, all SCREEN points and selected
key, CONFIRM outcomes, paired cells, Wilson values, disclosure/recount, gates,
five-file inventory, unrun stages, and bounded scope. Do not mark your own work
accepted and do not commit or push.
