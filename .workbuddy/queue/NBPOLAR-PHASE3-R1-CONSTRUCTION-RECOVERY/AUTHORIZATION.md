# Phase 3-R1 authorization

Authorized by the user on 2026-09-11 as the new NB-Polar heavy execution task.

The execution session may autonomously:

- implement the scoped Phase 3-R1 construction recovery;
- run and revise focused tests and the accepted Phase 1/2 regressions;
- run TRAIN and DEV experiments using new, separated streams;
- compare at least three construction/gate options using TRAIN/DEV only;
- freeze the chosen construction, full 256-coordinate order, thresholds, exact
  executable command, fresh seed, output root, and stop rules;
- ask a real independent reviewer-go to review that frozen EVAL contract;
- after reviewer-go PASS, run exactly one fresh synthetic EVAL and complete the
  mandatory independent Pre-RESULT review.

The authorization does not allow reuse or rerun of seed 2026091203, mutation of
the predecessor diagnostic, Model-F, real data, Phase 4, benchmark/result roots,
scientific promotion, commit, or push. Any failed pre-EVAL review blocks EVAL
until the scoped defect is repaired and independently reviewed again.
