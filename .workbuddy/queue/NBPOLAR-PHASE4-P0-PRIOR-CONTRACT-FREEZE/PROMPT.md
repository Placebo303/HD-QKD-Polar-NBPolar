# Phase 4-P0 prompt — prepared, not authorized

Do not execute until `STATUS.yaml` explicitly authorizes Phase 4-P0.

When authorized, work autonomously from
`.workbuddy/queue/NBPOLAR-PHASE4-P0-PRIOR-CONTRACT-FREEZE/TASK_PACKET.md`.

This is a read-only inventory and mathematical freeze task. Produce the exact
Model-F-to-GF32 prior contract, axis/provenance table, CAL/DEV/EVAL separation,
oracle/candidate L2 diagnostic definitions, minimal adapter API, tests and
Phase 4-P1 implementation packet. Do not implement the adapter, read private
raw TTBin data, execute Model-F or a decoder, tune on VAL/EVAL, touch result
roots, or open reconciliation. Return one reviewed freeze candidate or one
exact blocker.
