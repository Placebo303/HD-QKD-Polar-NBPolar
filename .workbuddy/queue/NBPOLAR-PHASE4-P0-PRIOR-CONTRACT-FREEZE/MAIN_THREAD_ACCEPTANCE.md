# Main-thread acceptance — Phase 4-P0

Disposition: `FREEZE_ACCEPT`.

The Model-F-to-GF32 prior contract, explicit `[Alice,Bob]` axis convention,
per-Bob-column concentration formula, banned per-cell twin, 5+5 integer-label
packing, dense `SymbolMetric` MVP, six pure adapter helpers, CAL/DEV/EVAL
separation, truth-isolation rules, and V-P0-01 through V-P0-12 are accepted as
the Phase 4 implementation contract.

The selected next implementation slice is narrower than the candidate P1
draft: Phase 4-P1 implements only the pure adapter plus synthetic independent
formula-oracle qualification V-P0-01 through V-P0-07. It may not load CAL,
run Model-F or SC, or select DEV/EVAL identities. Those actions require a later
separate packet after P1 acceptance.

No scientific or performance result is accepted. Phase 4-P1 remains inactive
until the user sends its explicit authorization prompt.
