# Main-thread acceptance — Phase 6-R1

Verdict: `DECODE_REJECT_ADVANCE_DEVELOPMENT_ACCEPTED`.

The main thread accepts the single frozen three-arm synthetic result within its
registered scope. R1 restored 18 strict-stop losses with no regressions,
matched static recovery at 298/300, retained 23.59% lower average
key-dependent disclosure, and passed all 18 gates. Attempt 1/1 and run seed
2026091350 remain consumed; the five-file evidence root is immutable.

This accepts the continuation semantics and their single-layer BEC synthetic
development evidence only. It does not establish two-layer reconciliation,
reconciliation efficiency, a target block length, empirical-channel
construction, real-data FER, qualification or promotion.

Disposition: pause Phase 7/SCL. The next packet is Phase 6-R2 two-layer rate
feasibility, because the operational L2 candidate-conditioned path and a
numerical `f <= 1.3` gate are still missing.
