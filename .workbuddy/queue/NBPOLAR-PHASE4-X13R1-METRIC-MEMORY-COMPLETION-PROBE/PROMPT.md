Execute the authorized X13R1 Tier-X delta exactly. Keep X13 immutable, verify
and inherit its small-N evidence, freeze the new embedded body, then run the
four isolated child cells in order with checkpointing. Do not count X13 partial
large-N blocks toward R1. Write only the two new probe files, obtain independent
reviewer-go focused review in the return message, and make no claim or
production change. No commit/push.
