Implement and, only after independent reviewer-go Pre-EXECUTE PASS, execute
the complete P14 Tier-Y packet in `TASK_PACKET.md`. Preserve the frozen nested
prefixes, streams, one-open/one-attempt point, checkpointing, resource limits,
classification and STOP rules. Obtain independent reviewer-go Pre-RESULT
review before returning. Do not commit, push, self-accept or extend scope.

