Implement and, only after independent reviewer-go Pre-EXECUTE PASS, execute
the complete P15 Tier-Y packet in `TASK_PACKET.md`. Preserve its two N cells,
seed groups, one-open/one-attempt point, empirical-only construction,
checkpointing, resource limits, classification and STOP rules. Obtain
independent reviewer-go Pre-RESULT review before returning. Do not commit,
push, self-accept or extend scope.

