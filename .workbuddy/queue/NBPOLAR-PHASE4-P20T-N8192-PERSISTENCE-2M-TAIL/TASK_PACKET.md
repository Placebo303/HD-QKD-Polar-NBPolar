# Phase 4-P20T — N=8192 persistence probe on the 2M HOLD tail (FROZEN, awaiting explicit authorization)

- Tier: Y (decision-gate packet: Stage-A implementation + N=8192 construction/allocation/order derivation with ZERO protected reads, Stage-B single authorized execution).
- State: `FROZEN_AWAITING_EXPLICIT_AUTHORIZATION`. No implementation, derivation, protected read, decoder execution, or commit/push is authorized by this file.
- RN-1 freeze review: PASSED 2026-09-20 (main thread) — population/arm/K/boundary/gates/corrections/honest-scope verified. This freeze alters none of those pins.
- Standing pre-authorization 2026-09-20 covers THIS FREEZE (freeze-only packet authoring; zero protected opens, zero data contact).
- Predecessor: `NBPOLAR-PHASE4-P20S-R1-GATE-FIX` terminal `TARGET_EMPIRICAL_N32768_MERGED_MECHANISM_PROBE_2M_COMPLETE_ACCEPTED_DESCRIPTIVE` (accepted descriptive, 15/15 files, N=32768).
- Decision parents: `.workbuddy/queue/NBPOLAR-PHASE4-NEXT-BRANCH-DECISION.md` (D1–D4, D1-A spent on P20S); `.workbuddy/queue/NBPOLAR-REDUCED-N-FEASIBILITY.md` (population arithmetic, C1–C10 cost inventory, comparability verdict, gate class); OpenSpec delta `openspec/changes/formal-ir-nbpolar-phase4-p0/specs/nbpolar-reduced-n-persistence/` (spec.md + design.md + proposal.md); umbrella `tasks.md` reduced-N section (RN-1..RN-8).
- Structural template: P20S `TASK_PACKET.md` (§§0-16) for arm/IR/tag/ledger/gate rigor; `P20S_FREEZE.md` (§§9-11) for command/freeze pattern; P16 `TASK_PACKET.md` (P16-02) for the construction/allocation derivation pattern.
- Gate discipline: any standing long-horizon authorization does NOT collapse Tier-Y gates. Stage A and Stage B each need their own explicit pasted authorization text; this packet authorizes neither.
- Selected session: `type2_2M_20260121_183657` (2M), scoring population first-and-only use of the single N=8192 DEV block on the 2M HOLD tail (see §4). The 1M pool, the entire 1.5M session, 2M TRAIN-build, and all consumed 2M ranges are fail-closed against P20T DEV selection (see §4). 1.5M and 2M are never mixed. D1-A (P20S merge precedent) is NOT invoked.

## 0. Planner header (OpenSpec workflow)

- **Goal**: Record the single descriptive persistence observation available at N=8192 — three arms (newly derived N=8192 frozen-order-equivalent anchor α1, spike-local order candidate at identical K under the carried F-median8 formula-id with R=8 re-frozen for 8192-geometry, true-L1 oracle diagnostic), nine carried scalars + IR-1..IR-4 P20Q-identical + IR-5 N=8192-sized uncapped binary series per record, full P16-scale re-derivation (construction/allocation, K via the frozen budget formula with recomputed 2M H, fresh L1/L2/spike orders, new tag domain) — and pre-register within-N paired judgment quantities answering ONLY Q-G1/Q-G2 at N=8192.
- **Non-Goals**: No FER, reliability, efficiency, leakage, key-rate, recovery-rate, or scaling-superiority reading; no cross-N inference (no comparison against any N=32768 number); no H2 verdict or H2 input; no extension of any N=32768 chain; no K/order/prior absolute carried across N (all N=32768 literals stay at N=32768); no K recomputed from alt-H; no lambda anywhere; no floor-value change; no SCL/new kernel/model/schema; no fourth arm, second construction, construction sweep, or disclosure change; no derivation or sampling on real DEV frames at any stage; no calibration on DEV; no reuse of any consumed/closed range; no cross-session pooling (1.5M × 2M forever forbidden); no overwrite under `results/` or `comparison_bench/outputs_comparison/`; no commit/push.
- **Impact Scope**: New `FROZEN_N = 8192` runner (or thin-importer + new frozen constants) + focused injected tests + RN OpenSpec delta (`specs/nbpolar-reduced-n-persistence/spec.md` + `tasks.md` RN section, same umbrella change `formal-ir-nbpolar-phase4-p0`) + packet docs + Stage-A freeze supplement (new construction/allocation artifact + fresh order digests + spike digest + formula-id pin + derivation-program pin + K literals + set-delta table, recorded alongside this packet) + implementation notes + Stage-B evidence root (5 JSON/JSONL/md files + 1 IR-5 manifest + 9 N=8192-sized `.bin` series). No change to `src/`, `experiments/`, `tools/`, any accepted P16/P20O/P20Q/P20R/P20S root, X08/X09/X10 probe roots, H2 run root, or `results/` / `comparison_bench/outputs_comparison/`.
- **Acceptance Criteria**: P20T-R1..R9 (see §14), independent Pre-EXECUTE PASS + Pre-RESULT review on actual artifacts + main-thread acceptance. Descriptive-persistence label only; `undetected` isolated; oracle never operational; outcomes recorded per record but explicitly NOT read as recovery rates; H2 decision explicitly excluded; cross-N reading explicitly excluded.
- **Tasks**: (planner task list for coder agents) H1 freeze N=8192 derivation contract — TRAIN seeds/budget, K rule, R=8 re-freeze (§3, R1); H2 freeze single-segment population/gates (§4, R2); H3 freeze estimate-shaped caps from the in-packet K rule (§5, R3); H4 freeze arm pins — newly-derived frozen-order-equivalent A + spike-B (F-median8 carried, R=8 re-frozen) + derivation-program pin + oracle D-continuity (§§3/6, R4); H5 wire endpoints/taxonomy + mandatory nine scalars + IR-1..IR-4 (P20Q-identical) + IR-5 N=8192-sized binary series + manifest (§7, R5); H6 enforce one-shot budget/stop (§8, R6); H7 Stage-A suites green with zero-open audit (R8); H8 independent Pre-EXECUTE adjudication of derivation/K-literal/population/IR boundary (R7); H9 single authorized Stage-B attempt (R6/R7); H10 independent Pre-RESULT + acceptance (within-N geometry quantities checked present, never decided in-packet).
- **Honest scope** (binding on proposal/design/packet/return language, repeated verbatim in the return): a descriptive N=8192 persistence probe on one 2M HOLD-tail block; n=1; within-N paired comparison only (Q-G1/Q-G2); no FER/reliability/efficiency/leakage/key-rate/recovery/scaling/promotion claim; no cross-N inference; no H2 input; the DEV block is consumed by this packet; the HOLD remainder 3627..3644 and all 1.5M remainders stay untouched; any exact count is COMPLETE when integrity holds and is explicitly NOT read as a recovery rate.

## 1. Mission

With a FULLY NEW N=8192 derivation — construction/allocation re-derived per the P16 pattern from synthetic TRAIN-block sampling drawn ONLY from the worktree-reused prior arrays (Stage-A derivation under frozen seeds; zero counts-NPZ opens, zero real-frame use), fresh worst-first L1/L2 orders at length 8192, fresh spike-local order under the carried F-median8 formula-id with R=8 re-frozen for 8192-geometry, K derived in-packet from recomputed 2M H via the frozen budget formula with (K1,K2) by TRAIN residual, new tag domain — and everything else carried as shape-only (α1 rule, floor 1e-15, greedy SC only, IR-1..IR-4 formulas, nine-scalar semantics, gate machinery):

> On the ONE N=8192 DEV block (2M HOLD 3595..3626), does the spike-local L2 fail-site pattern recur within-N (Q-G1), and does the spike-local order disclose more top-hazard-tail mass than the frozen-order equivalent at the same N (Q-G2) — with the nine carried scalars and the N=8192-sized IR-5 series preserved per record, and with per-record outcomes recorded but explicitly NOT read as recovery rates?

P20S merged DEV (2M VAL 2827..2915 + HOLD 3556..3594) is CONSUMED; P20R 1.5M VAL-remainder DEV 2044..2171 CONSUMED; all earlier DEV ranges CONSUMED per the P20S §4 ledger (inherited here). Post-P20S never-decoded ledger: 1.5M VAL stub 2172..2212 (41 / 10496 pr), 1.5M HOLD 2725..2766 (42 / 10752 pr), 2M HOLD tail 3595..3644 (50 / 12800 pr). This packet touches ONLY the 2M tail's first 32 frames. All branches after this packet are deferred to §16 and must not be prejudged here.

## 2. Background and first-principles decision record (frozen, not re-argued in Stage A/B)

- Population constraint (feasibility §1, RN-1-verified): the 2M HOLD tail 3595..3644 holds 50 frames = 12800 pairs = exactly ONE N=8192 block (32 frames) + an 18-frame remainder. First-contiguous-block rule (same convention as all P20 DEV selections): DEV = 3595..3626, remainder = 3627..3644 (counted, never decoded). Single contiguous segment, single split (HOLD), single session — NO cross-split combining is needed and the D1-A merge precedent is NOT invoked.
- Session-difficulty evidence forces 2M (true-L1 oracle ceiling ~12.5% on 1.5M vs ~70% on 2M); the 1.5M stubs stay counted never-decoded as low-information controls only, never pooled, never primary evidence; 1.5M↔2M mixing forever forbidden.
- Probe form (RN delta ADDED-1/ADDED-3): n=1 descriptive persistence check with within-N paired judgment ONLY. The probed factor is the disclosed-L2 SET delta between arm A and arm B at identical K (size-delta exactly 0 by design). Q-G1/Q-G2 verbatim (§7); mining formulation, no thresholds, no votes.
- Reuse-vs-derive stance (feasibility C1–C10, design §1): at N=8192 NOTHING is thin-reusable except session-H inputs, formula shapes, and code paths. Effort class is P16-scale (new derivation + new/tested runner + full-suite green + two independent reviews + one Tier-Y execution), explicitly NOT P20S-scale thin reuse. The N=32768 order files, K literals (334/6746/7080), tag value (327743), IR-5 32768 geometry, and all leakage literals stay at N=32768 and SHALL NOT transfer (§7 comparability boundary; delta ADDED-7).
- Alternatives rejected in writing: (a) reuse N=32768 orders/K at N=8192 — length/ geometry mismatch, forbidden; (b) carry (K1,K2) from (334,6746) — forbidden, TRAIN-residual selection only; (c) recompute K from alt-H — forbidden; (d) any α other than 1, second construction, construction sweep — second factor, forbidden; (e) λ anywhere — X08-falsified, forbidden; (f) floor change — H3/H2d exonerated, forbidden; (g) SCL/list/belief decoder — decoder change, forbidden (SCL unlock runs on synthetic X11/D3, never on this block); (h) consume the 18-frame remainder, any 1.5M stub, any consumed population, or any 1M range — forbidden; (i) cross-session pooling — forbidden; (j) freeze any of the nine scalars or IR-1..IR-4 as absent — forbidden; (k) recovery-rate/FER/H2/cross-N reading of this packet's outcomes — explicitly excluded by form.

## 3. Reuse freeze + N=8192 derivation contract (normative)

- Counts source: NONE. Stage A performs ZERO protected opens (counts 0/0 at Stage-A close; the V25 counts NPZ is NEVER opened/statted/listed at any stage of this packet). FORBIDDEN derivation inputs: any 1M split, any 1.5M split, any 2M DEV/HOLD-DEV frames, Model-F CAL artifact, the V25 counts NPZ in any form.
- Reused products, read-only (digest-pinned, never re-derived, never carried as N-literals):
  - (p1) Worktree `raw_prior_2m.npz` ARRAYS as off-protected-data derivation inputs (same P20O queue-dir file, canonical digest `b16f52165d9f7ef28884df270f921ce07c2a743f4f4b39c3435838809ae1c587` replayed at Stage A by worktree-file recomputation ONLY); session H literals `0.02566204884275839 / 0.8069006731309893 / 0.8325627219737477` RECOMPUTED via accepted `target_construction.entropy_bits` within 1e-12 (never hand-filled per AGENTS.md §5.5; `beta_eff_empirical` never hand-filled).
  - (p2) Formula shapes: budget rule `floor((1.3·N·H−64)/5)`, tag-length rule `10·N+63`, K-split exhaustive TRAIN-residual selection, F-median8 ranking shape, IR-1..IR-4 formulas/caps, nine-scalar semantics.
  - (p3) Code paths: `sc.py`, `sc_chunked_gate.py`, `transform.py`, causal two-layer wiring, Toeplitz accounting, P20A resource/endpoint machinery, recount/gate machinery.
- NEW derivation at N=8192 (Stage-A scope, per P16 pattern P16-02):
  - (d1) Synthetic TRAIN-block sampling at N=8192 drawn ONLY from the worktree-reused prior arrays: TRAIN seed block `2026092410..2026092413` × 4 blocks per stream = **16 TRAIN blocks**, per-stream RNG restart, P13-shared true-prefix/oracle-conditioned genie rows. Block count (16) + derivation seeds (2026092410, 2026092411, 2026092412, 2026092413) PINNED IN THIS FREEZE following the P16 4-streams×4-blocks precedent. Freshness: repo grep for each of `2026092410|2026092411|2026092412|2026092413` returns zero matches (verified 2026-09-20 at freeze). Zero counts-NPZ opens; zero real-frame use; sampling budget 16 blocks only.
  - (d2) Pooled per-layer risks → worst-first `(e,h,index)` orders → fresh length-8192 L1/L2 empirical permutations + new `construction_and_allocation_8192.json`-class artifact (exact filename frozen at Stage A alongside this packet).
  - (d3) K rule: `K_total = floor((1.3·8192·H−64)/5)` DERIVED IN-PACKET from recomputed 2M H within 1e-12. Arithmetic estimate ≈1760 ONLY (1.3·8192=10649.6; ×H≈8866.5; −64≈8802.5; /5≈1760.5) — an ESTIMATE, never a committable literal; the −64 tag term breaks exact quartering (7080/4=1770). (K1,K2) by exhaustive TRAIN-residual selection, frozen before DEV; NEVER from (334,6746).
  - (d4) Spike order: carried F-median8 formula-id (`score[i] = h[i] − median(h over the R=8 clipped natural neighborhood of i)`, descending rank, ascending tie-break, deterministic) AND **R=8 re-frozen as a NEW freeze decision for 8192-geometry** (not an automatic carry — recorded explicitly here; the window convention is re-decided for the 8192 coordinate geometry). New length-8192 spike permutation + digest pinned at Stage A. Deterministic, zero sampling/genie/protected reads; inputs worktree prior arrays ONLY; L1 carried on all arms; first-K2 disclosed at identical size (size-delta zero; SET-delta IS the probed factor).
  - (d5) New runner (or thin-importer + new frozen constants) with `FROZEN_N = 8192`; the `target_construction.py:1559` hard gate (`n == FROZEN_N`) forbids passing `--n 8192` to any frozen-point runner. SC stage count log2(8192) = **13**; `chunk_rows` geometry re-sized per P11 precedent to **128** (512/4, preserving 64 chunks; frozen here, not tunable). All frozen sizing constants new.
- D1/D2 status: no feasibility recompute beyond the packet's own TRAIN-residual selection; the P20O D1/D2 2M literals are NOT carried (N-literals stay at N=32768). Stage A freezes the in-packet TRAIN-residual selection outcome + K literals BEFORE any DEV contact; a derivation-gate mismatch BLOCKS before any DEV open (DEV stays 0/1) and no Stage-B authorization is requested.
- Stage B loads the frozen Stage-A files read-only behind the construction-identity + order-identity + spike-identity + derivation-program gates (digests + alpha-1.0/floor pins + H-literal recomputation within 1e-12 + K-literal replay + formula-id pin with the R=8 re-freeze recorded, or BLOCKED before any sampling and before any SC call).

## 4. Population and closed/consumed-data rule (normative)

- The following are CONSUMED/CLOSED and SHALL NOT supply P20T blocks: every DEV range listed consumed in P20S §4 (P18/P19 HOLD blocks, P20B VAL pool, P20C/P20E/P20F, P20H–P20K, P20M VAL DEV, P20N HOLD DEV, P20O 2M VAL DEV 2187..2826, P20Q 2M HOLD DEV 2916..3555, P20R 1.5M VAL-remainder DEV 2044..2171, P20S merged DEV 2827..2915+3556..3594); P20L never executed (zero consumption). The ENTIRE 1M pool and the ENTIRE 1.5M session are fail-closed against P20T DEV selection (cross-file gate). 1.5M TRAIN 0..1659, 1.5M VAL 1660..2212, 1.5M HOLD 2213..2766 SHALL NOT be opened/statted/listed/read under this packet in any form. 2M TRAIN 0..2186 SHALL NOT supply P20T blocks (build frames). 2M VAL DEV 2187..2826, 2M HOLD DEV 2916..3555, and P20S merged DEV SHALL NOT be opened/statted/listed/read under this packet in any form.
- Single-block-first-and-only-use justification (same canonical TRAIN→held-out separation as P20M §4, frozen, not re-argued in Stage A/B): the DEV segment has never been decoded, tuned on, or scored by any EXECUTED NB-Polar packet; 2M TRAIN supplies the session hypotheses (worktree prior arrays from TRAIN-fitted counts; synthetic TRAIN sampling from those arrays only, never from DEV frames); using the DEV block as a one-shot scoring population is declared once, consumed once, never tuned, never returned to a validation role. The HOLD remainder stays undecoded.
- Stage B runs on the DECLARED population:

| item | frozen value |
|---|---|
| source file identity | `comparison_bench/outputs_comparison/nonbinary_diagnostics/v13r3fresh_pairs_20260816/type2_2M_20260121_183657/pairs.parquet` ONLY (same pairs parquet as P20S; provenance pin frozen in Stage A by manifest cross-check; mismatch blocks; planner performed zero opens/stats/listings) |
| split manifest identity | `comparison_bench/outputs_comparison/nonbinary_diagnostics/nbldpc_v25_20260818/run_04/split_manifest.json`, schema `nbldpc_v25_split_manifest_v1`, 2M TRAIN 2187/559872 + VAL 729/186624 + HOLD 729/186624 (same split manifest as P20S; JSON-metadata read only) |
| construction/order-derivation input | §3 worktree prior arrays ONLY (TRAIN-fitted; synthetic TRAIN sampling at N=8192 under frozen seeds; never a DEV frame) |
| DEV frame rule | HOLD `3595..3626` (32 frames), single contiguous segment, in (frame_id, pair_idx) order (Stage A confirms or replaces with written equivalent + justification + re-review; never assumed) |
| DEV blocks (N=8192) | ONE block: `3595..3626` (32 frames; 8192 pairs = ONE full block) |
| declared remainder | HOLD frames `3627..3644` (18 frames / 4608 pairs) recorded never used — counted, never decoded |
| 1.5M stubs | VAL stub `2172..2212` (41 frames / 10496 pairs) + HOLD `2725..2766` (42 frames / 10752 pairs) recorded never used — counted, never decoded, never contacted beyond counting; low-information controls only, never pooled, never primary evidence |
| build-frames disjointness (S2-ii) | derivation/scoring frames vs build ⊂ 2M TRAIN `0..2186`; DEV = HOLD `3595..3626`; disjoint by split identity; declared frame sets above; the TRAIN-exclusion gate refuses any overlap before any protected content open |
| consumed-1M exclusions | entire 1M pool (any split/subrange) — overlap refuses before any protected content open |
| consumed-1.5M exclusions | all 1.5M TRAIN `0..1659` + VAL `1660..2212` + HOLD `2213..2766` — overlap refuses before any protected content open |
| consumed-2M exclusions | 2M TRAIN `0..2186` as DEV; 2M VAL DEV `2187..2826`; 2M HOLD DEV `2916..3555`; P20S merged DEV — all in any form |
| block count | 1 at N=8192 |
| tag domains | new P20T domain (§6) |

- Fail-closed gate family (frozen in the runner, order (a)→(g), cross-file first):
  (a) CROSS-FILE 2M — DEV content-open path + stat-size/sha pin must equal the 2M session identity; any 1M or 1.5M path or digest mismatch refuses before any SC call (frame integers alone are never identity);
  (b) INTRA-FILE SINGLE-SEGMENT containment — the DEV segment must lie wholly inside HOLD 2916..3644 with zero TRAIN/VAL-DEV/HOLD-DEV overlap, else refuse before any protected content open;
  (c) CONSUMED-1M EXCLUSION — entire 1M pool;
  (d) CONSUMED-1.5M EXCLUSION — all 1.5M ranges above;
  (e) CONSUMED-2M EXCLUSION — 2M TRAIN-as-DEV + 2M VAL DEV + 2M HOLD DEV incl. the DEV∩build-frames disjointness declaration: DEV vs build ⊂ 2M-TRAIN with the frame sets above (S2-ii);
  (f) REUSE-PRIOR + NEW-CONSTRUCTION/ORDER-IDENTITY + K-RULE + SPIKE-DERIVATION-PROGRAM — §3 worktree-prior digest + H-literal recomputation within 1e-12 + new N=8192 construction/order file digests + K-rule derivation replay + frozen spike formula-id (with R=8 re-freeze) + derivation-program pin (module + function + formula-id + argv + sampling-budget attestation), else refuse before any SC call;
  (g) FRAME-SET-IDENTITY + ORDER-POSITION-IDENTITY + TAG-DOMAIN — the exact 32-frame list rule (3595..3626, in order) + the §6 order triple (newly-derived frozen-order-equivalent digest on A/O, new spike digest on B, in-packet K1/K2 literals, `--source 2M` vocabulary) + arm-specific order digests + P20T tag-domain pins must match, else refuse before any SC call.

## 5. Preregistered disclosure rule (normative — derived in-packet at Stage A, never hand-filled)

- K rule (same-session 2M H, fresh derivation): `K_total = floor((1.3·8192·H−64)/5)` with 2M H recomputed within 1e-12 at Stage A; `(K1,K2)` by exhaustive TRAIN-residual selection, frozen before DEV. The ≈1760 arithmetic estimate is an ESTIMATE ONLY, never a committable literal. NO recomputation from alt-H; NO hand-fill (AGENTS.md §5.5); NO new sampling beyond the §3 frozen 16-block budget.
- Caps PREREGISTERED at Stage A from the in-packet K (shapes frozen here, literals frozen in-packet):

| arm | K1 | K2 | key-dependent bits/block (shape) | public bits/tag |
|---|---|---|---|---|
| A_anchor_frozen_order_equivalent (operational) | K1 | K2 | 5·(K1+K2)+64 (≈8864 at the ≈1760 estimate) | 81983 |
| B_spike_local_order (probe candidate) | same K1 | same K2 | same as A (Δ vs A exactly 0) | 81983 |
| O_true_l1_oracle (diagnostic) | 0 | K2 | 5·K2+64 (in-packet literal) | 81983 |

- Tag value frozen arithmetic: `10·8192+63 = 81983` bits/tag (one 64-bit tag per record; same tag-bits semantics as P20S at the new length).
- Planned totals (derived at Stage A from the in-packet integers): key-dependent `2·(5·K_total+64) + (5·K2+64)` bits; public `3·81983` bits. B-vs-A key-bit delta is exactly 0 (order factor carries ZERO disclosure-size delta by design).
- Recount rule: independent key/public/tag recount, mismatch 0, or the gate BLOCKS. Per-record fields pin the session point (`prior_source` raw/2M-reuse, `k1/k2/k_total`, `budget_rule_replayed`, `construction_digest_8192`, `l1_order_digest`, `l2_order_digest` (arm-specific: newly-derived frozen-order-equivalent on A/O, new spike on B), `floor_hit_rate`).

## 6. Arms (frozen; order is the ONLY delta between operational arms; oracle is the P20O/P20Q-D continuity)

- `A_anchor_frozen_order_equivalent`: frozen greedy SC with incumbent `p1` + α1 `p2_alt`, NEWLY DERIVED N=8192 worst-first L1+L2 order prefixes (first-K1 / first-K2 of the Stage-A orders) at in-packet K1/K2 (operational anchor = P16-pattern construction + P20O-rule order continuity at the new N; 2 SC + 1 P20T-domain tag per block). "Frozen-order equivalent" = the SAME derivation RULE as the P20O 2M orders applied fresh at N=8192 — it SHALL NOT reuse the N=32768 order files in any form.
- `B_spike_local_order`: frozen greedy SC with incumbent `p1` + α1 `p2_alt` (byte-identical tables to A), SPIKE L2-order positions (first-K2 of the Stage-A length-8192 spike order under the carried F-median8 formula-id with R=8 re-frozen) with the SAME derived L1 order and SAME K1/K2 (probe candidate; 2 SC + 1 tag per block). Construction, K sizes, floor, decoder all byte-identical to A; the disclosed-L2 SET delta IS the probed factor.
- `O_true_l1_oracle`: true-L1-conditioned diagnostic with `p2_alt` + newly-derived order at carried K2 (provenance ORACLE, deployable=false, excluded from every operational aggregate; never a correction result; 1 SC + 1 P20T-domain tag per block). Order/table pin (default, frozen here): newly-derived frozen-order-equivalent + alt-L2 tables — the P20O/P20Q arm-D continuity. Order-factor attribution lives in the A-vs-B operational pair ONLY; O never enters any operational aggregate.
- No other arms. Block-major (A, B, O) on the single DEV block; checkpoint after every (arm, block) record; 3 records total. Kernel/representation/transform/SC arithmetic unchanged; greedy SC only; no SCL/new kernel/model/schema. Stage A performs sampling ONLY under the §3 frozen 16-block synthetic TRAIN budget (zero sampling at scoring; zero genie at scoring); Stage B performs pure DEV scoring (5 SC + 3 tags) with ZERO sampling — Stage-A scoring-time sampling 0, Stage-B sampling 0.
- Tag domain (new P20T): master 2026092400, prefix `nbpolar-p20t-n8192-persistence-2m-seed`, seed string `<prefix>:<master>:<n>:<arm>:<block_index>:<counter>` (SHA-256, MSB-first, truncated to `10·N+63 = 81983` bits). Prefix/master/arm tokens differ from ALL of P16/P17/P18/P19/P20B/P20C/P20E/P20F/P20G/P20H/P20I/P20J/P20K/P20M/P20N/P20O/P20Q/P20R/P20S/X14/X15/X16/X17 (incl. masters 2026092280/2026092300/2026092310/2026092330/2026092340/2026092360/2026092369/2026092380 and the 2026092390..2399 X17 range). Stage A verifies by repo grep that master 2026092400 and focused-test seeds `2026092401..2026092407` appear in no tracked file outside the P20T runner/tests/packet/spec documents (planner pre-checked `2026092400|...|2026092407` + `2026092410..2413` + `nbpolar-p20t` absent repo-wide 2026-09-20 — zero matches; zero protected access).
- Focused-test seeds `2026092401..2026092407` (7 seeds, mirroring the P20S 7-seed shape); derivation TRAIN seeds `2026092410..2026092413` (§3). Disjointness holds by the grep rule above. Stage B takes NO derivation seeds and performs no sampling.
- L2 disclosed-set rule (single-factor guard): arms A/O disclose the first-K2 positions of the SAME newly-derived N=8192 frozen-order-equivalent file (digest-gated `<FROZEN_AT_STAGE_A>`); arm B discloses the first-K2 positions of the SAME new spike order file (digest-gated `<FROZEN_AT_STAGE_A>`); the L1 set is the first-K1 of the SAME derived L1 order on all arms. Positions are fixed by the frozen files, never selected on closed blocks, consumed ranges, or DEV data. K sizes frozen identical on A/B; the SET-delta between the A and B L2 lists is recorded byte-exact (symmetric difference + rank-displacement table, descriptive only).

## 7. Thresholds, endpoints, and mandatory instrumentation (within-N geometry/coverage only — no recovery-rate reading, no cross-N reading)

- Outcome label `TARGET_EMPIRICAL_N8192_TAIL_PERSISTENCE_PROBE_COMPLETE` iff ALL integrity gates (§9) hold — regardless of per-record outcomes. ANY outcome pattern is COMPLETE when integrity holds.
- Choice + justification: within-N geometry/coverage-descriptive (no recovery/FER/Wilson threshold). This is a single-block persistence probe at a new N with fresh derivation — n=1 counts cannot discriminate arms (P20R demonstrated at N=32768), so there is no empirical basis for ANY count threshold here. A 62/64-class gate would be an invented threshold, explicitly forbidden. Branch and H2 decisions are excluded from this packet entirely (§16).
- NO recovery-rate reading, NO threshold votes, NO FER / superiority / qualification / promotion claim, NO cross-N inference (no comparison of any N=8192 number against any N=32768 number — the 1/4→2/5→4/5 chain, H2 verdicts, IR-5 32768 geometry, and all leakage literals stay at N=32768 per delta ADDED-7). Outcomes (`exact` vs non-exact) are recorded per record for completeness but explicitly NOT read as recovery rates. The pre-registered judgment quantities are within-N geometry ONLY (see below); any (or no) outcome pattern counts as COMPLETE. No efficiency language anywhere (CE ratios never called efficiency).
- Status taxonomy frozen: `exact` (tag-verified: outcome exact AND tag_pass AND label_match) vs `verify_failed`, `undetected` isolated (never success), plus `decode_failed` / `nonfinite` / `resource_abort` via the P20A path. P20A four-endpoint separation (`l1_exact` / `hard_l2_exact` / `oracle_l2_exact` / `pair_exact`) per record. First-error coordinate/layer rows (natural block symbol index) recorded per record. Per-record floor-hit fields (`floor_hits`, `floor_hit_rate`) complete per record (S2 reporting).
- Carried nine per-record SCALARS (scalar fields intact, arm-specific order digest): `l2_order_digest` (= newly-derived frozen-order-equivalent digest on A/O records, new spike digest on B records, gated), `l2_prefix_len` (= in-packet K2 on every record, gated), `l2_fail_in_prefix`, `l2_fail_hazard_bits`, `l2_fail_nbhd_mean_bits` (frozen radius R=8 window, clipped to the block), `l2_prefix_hazard_mean_bits`, `l2_fail_nbhd_floor_frac`, `l2_prefix_floor_frac`, `l2_fail_in_prefix_u_domain` (frozen PRESENT). Semantics identical to P20Q §7 (true cells under the record's own arm table; failing fields null unless `first_error_layer == L2`; prefix-wide means on every completed record; hazards = `-log2` arm-table mass at each position's true `(U1_cond, B, U2)` cell with `U1_cond` = hard-L1 candidate on A/B, true high on O).
- Mandatory IR-1..IR-4 (ALL FROZEN PRESENT, same caps/formulas as P20Q §7; Stage A pins exact formulas with injected vectors; never post-hoc):
  - IR-1 per-record 64-bin log-spaced hazard histograms {prefix, outside} using true cells (~1 KB/record): fields `ir1_hist_edges_bits` (65 float64, log-spaced over hazard-bits, frozen formula), `ir1_hist_prefix_counts` (64 int), `ir1_hist_outside_counts` (64 int). Split by disclosed L2 prefix membership (X-domain, first-K2 of the RECORD'S OWN order file — newly-derived frozen-order-equivalent on A/O, new spike on B); hazards under the record's own arm table. Every completed record (never null).
  - IR-2 hazard-rank percentile of the first-error coordinate (1 float): field `ir2_first_error_hazard_rank_pct` (float64 in [0,1], 1.0 = most hazardous; fraction of block positions with hazard <= fail-site hazard; null unless `first_error_layer == L2` with computable hazard, else null with frozen nullability).
  - IR-3 above-threshold prefix counts (≤3 scalars × ≤2 frozen thresholds): EXACTLY two thresholds, per-record deterministic, no tuning — `ir3_thresh_lo_bits` = 1.0 × record `l2_prefix_hazard_mean_bits`, `ir3_thresh_hi_bits` = 2.0 × record `l2_prefix_hazard_mean_bits` (frozen multipliers, P20Q-carried; deterministic per-record, frozen before the run, never fed back). Fields (6 scalars): `ir3_thresh_lo_bits`, `ir3_thresh_hi_bits`, `ir3_prefix_above_lo_count`, `ir3_prefix_above_lo_frac`, `ir3_prefix_above_hi_count`, `ir3_prefix_above_hi_frac` (prefix positions using true cells under the record's OWN order; every completed record, never null).
  - IR-4 top-k hazardous positions with ranks (k≤16, frozen k=16): fields `ir4_topk_coords` (16 int, natural block symbol indices), `ir4_topk_hazard_bits` (16 float), `ir4_topk_in_prefix` (16 bool/int, X-domain prefix flags under the record's OWN order), `ir4_topk_ranks` (16 int, 1-based hazard ranks). Hazard-order listing only; error-coordinate join is post-hoc. Every completed record (never null; N=8192 always fills 16).
- IR-5 N=8192-sized — full-block 8192-position series preserved (same uncapped scope convention as P20S, at the new N). Per record, EXACTLY three binary little-endian series + manifest linkage (NO text-JSON float dumps; NO `*.npz`/`*.npy`/`*.parquet`):
  - `<arm>_hazard_bits_f32le.bin`: float32 LE [8192] = 32768 B — true-cell hazard bits in natural block order under the record's own arm table (`U1_cond` = hard-L1 candidate on A/B, true high on O; same hazard definition as the nine scalars/IR recorders).
  - `<arm>_inprefix_u8.bin`: uint8 [8192] = 8192 B — X-domain disclosed-prefix flags under the record's OWN order (newly-derived frozen-order-equivalent on A/O, new spike on B).
  - `<arm>_inu_u8.bin`: uint8 [8192] = 8192 B — U-domain flags under the record's own arm table (the full-block form of the carried ninth scalar).
  - `ir5_full_manifest.json`: per-file sha256 + shape + dtype + endianness + order-identity (newly-derived frozen-order-equivalent vs new spike digest) + record linkage + format version (frozen `ir5full-v1`); digests cross-checked by the `ir5_full_manifest_identity` gate.
  - Size budget (frozen): 32 KiB hazard series + 8 KiB + 8 KiB = 48 KiB per record; 3 records ≈ 144 KiB; full Stage-B root ≤ ~1.5 MB; every committed file ≤ ~2 MB with the largest single file at 32768 B. The JSONL records carry IR-5 manifest REFERENCES (file names + digests), never inline float arrays.
  - Writer code point (frozen at Stage A): the new N=8192 runner's full-block writer (same callsite pattern as the P20S `_ir5_full_block_writer` — called post-decode after tag scoring, alongside the nine carried scalars and the IR-1..IR-4 recorder).
  - Truth-isolation boundary (normative): block truth (high/low/bob) and the arm tables enter the IR writers for RECORDING ONLY after all SC calls for the record have completed; outputs are written into the record dict / `.bin` files and never passed to any disclosure/order/sampling decision. A focused truth-isolation sentinel test pins this boundary (mutating truth changes no metric/decision input).
- Judgment form — pre-registered within-N questions ONLY (verbatim mining formulation; checked present, never decided in-packet):
  - Q-G1 = does the spike-local L2 fail-site pattern (first error at an undisclosed below-mean coord-class site with floor-rate elevation, paired against the frozen order) recur at N=8192;
  - Q-G2 = does the spike-local order disclose more top-hazard-tail mass than the frozen-order equivalent at the same N.
  - The accepted run must supply (i) per-record full-8192 mean/tail mass under the record's own order (from the IR-5 bins); (ii) spike-order coverage of disclosed-set mismatch positions — the byte-exact A-vs-B symmetric-difference set characterized by its hazard-rank distribution under each order (purely positional; NOT an outcome comparison); (iii) IR-1 tails under both orders (prefix vs outside tail-mass contrast A vs B); (iv) IR-4-style concentration at full-block scope (in-prefix fractions over top-128 / top-1024 hazard ranks from the series, per record's own order); plus the nine carried scalars (with arm-specific order digests) and the byte-exact A-vs-B set-delta table. Within N=8192 only; n=1 descriptive; no cross-N inference.

## 8. Budget, stop rules, SCL gate

- Single attempt: `attempts_allowed: 1`, `attempts_used: 0` until authorized execution; no rerun, no seed change, no prior/budget/order/construction tuning after preregistration. Execution-error rerun only as a recorded repeat of the identical freeze, never as tuning. P20S/R1 spent attempts do NOT transfer (independent packet).
- Reads (split-counted): counts-calibration opens 0/0 at every stage (no derivation input is ever a counts file; the V25 counts NPZ is never opened/statted/listed) + DEV open 1/1 reserved for Stage B (parquet, consumed at first DEV content open). 1M reads 0; 1.5M reads 0; 2M non-DEV reads 0 (HOLD remainder + all 1.5M stubs counted never-decoded, never contacted beyond counting). Any reopen, new calibration/derivation on real frames, real-frame use for derivation, or DEV refit is forbidden. Prior-array loads and the synthetic TRAIN sampling are worktree-file reads, not protected opens.
- SC/tag/genie budget: Stage-A derivation sampling ONLY under the §3 frozen 16-block budget + Stage-B pure DEV 5 SC (1 block × (2+2+1)) + 3 tags + 3 records with ZERO sampling at scoring (Stage-A scoring-time sampling 0, Stage-B sampling 0); derived per-record recomputation must equal counters, else BLOCKED.
- Resource-stop: P20A passthrough authoritative; abort preserves evidence + accounting; abort is BLOCKED, never success. Wall/RSS ceilings frozen in the Stage-A freeze supplement (P20S-class reference at the 5-SC / 3-tag budget; external timeout 1200 s + virtual/RSS caps 2 GiB + single thread frozen).
- Strategy stop rules binding: no tuning on closed blocks or the new DEV after the one shot; no reuse of consumed 1M pool (any split/subrange), any consumed 1.5M range (TRAIN 0..1659, VAL DEV 1660..2043, stub 2172..2212, HOLD 2213..2766), consumed 2M TRAIN-as-DEV / VAL DEV 2187..2826 / HOLD DEV 2916..3555 / P20S merged DEV, HOLD remainder 3627..3644 beyond counting, or any other session; no second factor after any probe pattern; no oracle-as-operational; no population-reliability inference from this single block alone; no recovery-rate reading of this packet's outcomes; no cross-N reading; no tuning inside this packet; no H2 input inside this packet.
- SCL entry gate UNCHANGED (unlocks nothing by itself; SCL work runs on synthetic X11 per D3, never on this block).

## 9. Evidence matrix and accounting (Stage-A freeze + Stage-B fifteen files)

- Stage-A freeze pins (zero protected opens): worktree-prior digest replay + H-literal recomputation + floor pins; K-rule derivation display + (K1,K2) TRAIN-residual selection outcome + literals; new N=8192 construction artifact path + digest; fresh L1/L2 order digests; spike order artifact path + file-bytes digest + formula-id pin (with R=8 re-freeze recorded) + derivation-program pin (module + function + formula-id + argv + sampling-budget attestation) + A-vs-B set-delta table; exact module path + N-flag Stage-B command verbatim + population integers + caps + budgets + tag domain + P16 construction digest lineage (read-only reference, NOT reused as construction) + IR-3 threshold multipliers (1.0×/2.0×) + IR-4 k=16 + IR-5 8192-sizing/encoding pins; recorded alongside this packet. Stage-A product identities: THREE new artifacts under this queue dir (N=8192 construction/allocation file, fresh L1/L2 orders file, new spike order file; digests pinned at Stage A); reuse paths point at the P20O queue dir (prior digest pinned alongside this packet).
- Stage-B root: `.workbuddy/queue/NBPOLAR-PHASE4-P20T-N8192-PERSISTENCE-2M-TAIL/n8192_persistence_probe_2m/` (exactly fifteen files; per-(arm, block) checkpointing; pure DEV scoring — zero sampling; one DEV content open; no reopen/rerun). Root must be ABSENT at Pre-EXECUTE.
- Fifteen files: `frozen_plan.json`, `input_and_predecessor_identity.json` (worktree-prior digest-gate proof + new-construction digest-gate proof + fresh-order digest-gate proofs (file bytes sha256 equal the frozen digests) + spike digest-gate proof + derivation-program pin (module + function + formula-id + argv + sampling-budget attestation) + formula-id pin with R=8 re-freeze + A-vs-B set-delta table + K-literal replay + budget-rule replay + tag-domain pins + split manifest + DEV integers + DEV∩build-frames disjointness declaration with frame sets + IR-3 multiplier pins + IR-4 k pin + IR-5 8192-sizing/encoding pins), `per_block_arm_outcomes.jsonl` (3 records at completion, each carrying the nine scalars with arm-specific order digest + IR-1..IR-4 payload with frozen nullability/caps + IR-5 manifest references), `aggregate_summary.json` (incl. the within-N Q-G1/Q-G2 tables: full-block series summaries + mismatch-coverage table + IR-1 tails under both orders + full-scope top-128/1024 concentration + A-vs-B set-delta table — all explicitly labelled non-recovery, within-N only), `report.md`, `ir5_full_manifest.json` (per-file sha256 + shape + dtype + endianness + order-identity + record linkage + `ir5full-v1`), plus the nine IR-5 `.bin` files (`A_hazard_bits_f32le.bin`, `A_inprefix_u8.bin`, `A_inu_u8.bin`, `B_…×3`, `O_…×3`). Per-record §7 endpoints + first error coordinate/layer, zero-count hits, floor hits + floor-hit rate + log loss, true-H vs candidate-H L2 NLL, taxonomy, construction-point fields (`l2_construction` alt-α1 on all arms, `k1/k2/k_total`, `prior_digest`, `construction_digest_8192`, `l1_order_digest`, `l2_order_digest` arm-specific) + the NINE carried scalars + IR-1..IR-4 fields (all PRESENT per the Stage-A freeze) + IR-5 file references with digests.
- Accounting: key/public/tag disclosure + independent recount (mismatch 0); sampling call counts (Stage-A derivation = frozen 16-block budget only; Stage-B 0) + SC counts (L1/L2 split: A 2 / B 2 / O 1) + tag counts exact; block SER/NLL (recorded, never a reliability claim); wall/RSS; per-arm floor-hit rate + per-arm prefix hazard means + within-N Q-G1/Q-G2 tables reported. NO per-arm exact counts are read as recovery rates; the A→B transition cells are NOT computed (counting vocabulary is out of scope for this packet); NO cross-N comparison is computed.
- Integrity gates in frozen order (all true or BLOCKED): `predecessor_r1_root_identity`; `reuse_prior_arrays_identity` (worktree npz canonical digest + floor pins + H literals within 1e-12, worktree-file only); `new_construction_identity_8192` (Stage-A construction file-bytes digest + sampling-budget attestation + TRAIN-seed pins + (K1,K2) TRAIN-residual selection replay); `fresh_order_identity_A_8192` (fresh L1/L2 order-file digest, Stage-B file-bytes equality replay-exact, permutation-valid + K-prefix checks, zero scoring-time sampling); `spike_order_identity_B_8192` (Stage-A spike file-bytes digest, Stage-B equality replay-exact, permutation-valid + K2-prefix-disclosed + L1-carried checks, formula-id + R=8 re-freeze pin); `order_derivation_identity` (derivation-program pin + formula-id pin + reused-prior-digest input pin + sampling-budget pin); `k_rule_derived` (budget-rule display + H recomputation within 1e-12 + K literals + TRAIN-residual selection replay; NEVER a 1.5M absolute, never from alt-H); `target_population_contract`; `dev_population_exact` (32 DEV frames / 8192 pairs / 256 rows per frame / pair indices / symbol range, single HOLD segment); `blocks_exact_with_declared_remainder` (ONE exact DEV range per arm in block-major slot order + declared HOLD remainder 18 frames / 4608 pairs + counted-never-decoded 1.5M stubs 83 frames / 21248 pairs); `three_records_exact`; `sampling_calls_exact` (Stage-A derivation = 16-block budget; Stage-B 0); `sc_calls_exact` (derived 5); `tags_exact` (derived 3); `orders_valid_k_prefixes_within_registered_arms` (fresh-order prefixes on A/O + spike prefixes on B + construction triple α1×3 + K literals); `order_set_delta_recorded` (A-vs-B L2 disclosed-set symmetric difference + rank-displacement table byte-exact, size-delta exactly 0); `hazard_instrumentation_complete` (all nine §7 scalars present with correct nullability + arm-specific order digest on every completed record); `ir_payload_complete` (IR-1..IR-4 all PRESENT with frozen caps/nullability + IR-5 N=8192-sized: nine `.bin` files byte-present with manifest digests matching on every completed record); `ir5_full_manifest_identity` (per-file sha256 + shape + dtype + endianness + order-identity + `ir5full-v1`, all equal); `evidence_size_rule_met` (every committed file ≤ ~2 MB; largest single file 32768 B); `floor_hit_rate_reported`; `oracle_isolation`; `buckets_disjoint_exhaustive` (unique arm/block + schema); `undetected_zero`; `nonfinite_zero`; `truth_isolation` (incl. the IR recorder sentinel); `disclosure_recount_exact`; `one_open_per_protected_input` (counts 0 + DEV 1); `input_stat_unchanged`; `no_unregistered_access`; `resource_limits_met_and_no_abort`.

## 10. Commands (exact argv frozen in Stage A / Stage-A freeze)

- Stage A (implementation + N=8192 derivation + injected tests + reuse-freeze; authorized separately):
  `.venv/bin/python -m pytest comparison_bench/tests/test_nbpolar_n8192_persistence_probe_2m.py -p no:cacheprovider`
  using injected/synthetic inputs and a fresh additive `workspace/p20t/<uuid>/` temp root; protected-open audit declared separately (counts 0 + DEV 0 at Stage-A close). Safe smoke per `AGENT_PROJECT_MEMORY.md` §4 stays valid.
- Stage-A derivation + verification (ZERO protected opens; worktree-file digest recomputation + synthetic TRAIN sampling ONLY): recompute the worktree prior canonical digest + H literals from the frozen worktree files; run the frozen derivation program under the frozen TRAIN seeds (§3 d1) to produce construction/allocation + fresh orders + spike order; pin all digests + formula-id + program pin + set-delta table; confirm the §4 DEV integers against the split-manifest JSON metadata (no parquet/NPZ open/stat/listing); grep-verify the §6 tag/test/derivation-seed freshness. Template (pins filled at Stage A; all flags required, no production default):
```bash
cd /mnt/d/Code/HD-QKD_Polar_Comparison-nbpolar
export OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 MALLOC_ARENA_MAX=2
<STAGE_A_MODULE_PATH> --verify-derivation --prior .workbuddy/queue/NBPOLAR-PHASE4-P20O-2M-MAINTAIN-CONFIRMATION/raw_prior_2m.npz --prior-digest b16f52165d9f7ef28884df270f921ce07c2a743f4f4b39c3435838809ae1c587 --construction <FROZEN_AT_STAGE_A> --construction-digest <FROZEN_AT_STAGE_A> --order-file <FROZEN_AT_STAGE_A> --order-digest <FROZEN_AT_STAGE_A> --spike-order-file <FROZEN_AT_STAGE_A> --spike-order-digest <FROZEN_AT_STAGE_A> --spike-formula F_MEDIAN8_CARRIED_20260920_H_MINUS_LOCAL_MEDIAN_W8_R8_REFROZEN_8192 --k1 <FROZEN_AT_STAGE_A> --k2 <FROZEN_AT_STAGE_A>
```
  Stage A confirms the module path (new `FROZEN_N = 8192` runner, or thin-importer + new frozen constants with justification) and the `--source` vocabulary (only `2M`). Forbidden by default: `longrun_*`, `minrerun_*`, `routeA_*`, `experiments/run_e2e_pipeline.py` on raw data, full sweeps.
- Stage B execution command (RECOMMENDED here, frozen verbatim at Stage A with the `<FROZEN_AT_STAGE_A>` pins filled; NOT AUTHORIZED until independent Pre-EXECUTE PASS + pasted Stage-B authorization):
```bash
cd /mnt/d/Code/HD-QKD_Polar_Comparison-nbpolar
export OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 MALLOC_ARENA_MAX=2
ulimit -v 2097152
timeout 1200 <STAGE_A_MODULE_PATH> --prior .workbuddy/queue/NBPOLAR-PHASE4-P20O-2M-MAINTAIN-CONFIRMATION/raw_prior_2m.npz --prior-digest <FROZEN_AT_STAGE_A> --source 2M --floor 1e-15 --n 8192 --k1 <FROZEN_AT_STAGE_A> --k2 <FROZEN_AT_STAGE_A> --construction <FROZEN_AT_STAGE_A> --construction-digest <FROZEN_AT_STAGE_A> --dev-pairs comparison_bench/outputs_comparison/nonbinary_diagnostics/v13r3fresh_pairs_20260816/type2_2M_20260121_183657/pairs.parquet --dev-frames-hold 3595 3626 --block-frames 32 --remainder-frames 3627 3644 --tag-master 2026092400 --chunk-rows 128 --tag-bits 64 --order-file <FROZEN_AT_STAGE_A> --order-digest <FROZEN_AT_STAGE_A> --spike-order-file <FROZEN_AT_STAGE_A> --spike-order-digest <FROZEN_AT_STAGE_A> --spike-formula F_MEDIAN8_CARRIED_20260920_H_MINUS_LOCAL_MEDIAN_W8_R8_REFROZEN_8192 --out-dir .workbuddy/queue/NBPOLAR-PHASE4-P20T-N8192-PERSISTENCE-2M-TAIL/n8192_persistence_probe_2m
```
  All flags required, no production default; three hardcoded arms with the order swap pinned (never CLI-tunable: A = α1 + newly-derived frozen-order-equivalent at in-packet K; B = α1 + new spike order at in-packet K; O = newly-derived-order true-L1 oracle at carried K2); `--alpha` takes no Stage-B value (construction is frozen in the Stage-A files). Stage A fills every `<FROZEN_AT_STAGE_A>` pin with the §3 derived values plus the `--source` vocabulary (only `2M`) and the IR-3 multiplier + IR-4 k + IR-5 8192-sizing pins in `frozen_plan.json`. The single-segment frame flags (or a written equivalent frozen at Stage A) enforce gate (g). Forbidden by default as above.
- Read-only verify: reviewer recomputes §9 identities from artifacts; never `git show HEAD:` blobs for evidence paths (P19 lesson).

## 11. Products

- Stage A: THREE new artifacts under this queue dir (N=8192 construction/allocation file + fresh L1/L2 orders file + new spike order file; each + file-bytes digest + formula-id pin (with R=8 re-freeze) + derivation-program pin + A-vs-B set-delta table + sampling-budget attestation) + reuse-verification freeze supplement (§3 digest/H recomputation + K-rule derivation display + TRAIN-residual selection outcome + fresh-order digests + spike digest + formula-id + program pin + D-status + population integers + caps + budgets + tag domain + IR-3 multipliers + IR-4 k + IR-5 8192-sizing pins + module path + all commands verbatim) + implementation notes (exact files, diffs, test commands/results, frozen derivation/population/cap/command) + new runner + focused injected tests. NO other new artifacts (reuse paths point at the P20O queue dir).
- Stage B (only after authorization): evidence root (§9, fifteen files) + `OPERATOR_RETURN.md` (+ freeze supplement, `PRE_EXECUTE_REVIEW.md`, `PRE_RESULT_REVIEW.md`, `MAIN_THREAD_ACCEPTANCE.md` at gates).
- OpenSpec RN delta: `openspec/changes/formal-ir-nbpolar-phase4-p0/specs/nbpolar-reduced-n-persistence/spec.md` + `tasks.md` RN section (same umbrella change as P16/P20X; no new top-level change).

## 12. Allowed work

- New `FROZEN_N = 8192` runner (or thin-importer + new frozen constants with justification) + N=8192 derivation program (worktree-prior-only synthetic TRAIN sampling under frozen seeds + deterministic spike permutation, §3 contract, carried F-median8 formula-id with R=8 re-frozen) + focused injected tests (temporary roots only).
- Read-only reuse (no logic change) of predecessor helpers by filename: `operational_f13.py` (construction/allocation pattern reference), `per_session_calibration.py` (digest recipe pin only), `l2_alt_maintain_2m.py` / `l2_alt_hold_ir_2m.py` (gate/record-writer/nine-scalar pattern references), `l2_mechanism_probe_2m.py` (spike-derivation + IR-5 writer pattern references), `l2_order_position_1p5m.py` (dual-order + derivation-program pattern references) (+ `construction.py` / `prior.py` / `sc.py` contracts as called).
- ZERO protected opens in Stage A except the frozen derivation-budget accounting: worktree-file digest recomputation of the §3 prior arrays + synthetic TRAIN sampling from those arrays under the frozen seeds/budget + split-manifest JSON-metadata confirmation of the §4 integers + grep-verification of the §6 tag/test/derivation-seed freshness + closed-form IR formula pins on injected vectors (zero scoring-time sampling, zero genie at scoring, zero real frames).
- RN OpenSpec delta + packet docs + Stage-A freeze supplement/return/review files + Stage-B evidence root (root only after Stage-B authorization) + the §3 reuse identity + the §3 derivation identity (§§3/9).

## 13. Forbidden work

- Any NPZ/parquet content open or stat/listing of DEV/remainder segments, 2M VAL-DEV, 2M TRAIN, 2M HOLD-DEV, any 1M split, or any 1.5M split in Stage A beyond the §3 frozen worktree-file reads and JSON-manifest metadata (Stage-A protected opens: counts 0/0, DEV 0/1); any counts-NPZ open at any stage; any K carried as an absolute from 1.5M or N=32768 or recomputed from alt-H (in-packet derivation only); any derivation or sampling on real DEV frames at any stage; any decoder execution before Pre-EXECUTE PASS + pasted Stage-B authorization; any spike derivation under any formula other than the carried F-median8 with R=8 re-frozen.
- Any change to GF32/transform/SC arithmetic, floor value, tag scheme semantics, outcome precedence, accepted evidence roots, P20O products, P20Q/P20R/P20S products, X08/X09/X10 probe roots, H2 run root, or `src/` + `experiments/` + `tools/` frozen baseline. No disclosure-size change (B−A = 0); no order change beyond the single spike L2 position set; no decoder change.
- No K/floor/order/decoder/step/success-point selection on closed blocks, the consumed 1M pool (any split/subrange), any consumed 1.5M range (TRAIN 0..1659, VAL DEV 1660..2043, stub 2172..2212, HOLD 2213..2766), consumed 2M TRAIN-as-DEV / VAL DEV 2187..2826 / HOLD DEV 2916..3555 / P20S merged DEV, HOLD remainder 3627..3644 beyond counting; no second construction, no construction sweep, no bounded search, no second order beyond the single spike set, no disclosure change; no tuning; no new-block peeking before the authorized attempt; no calibration on DEV; no H2 input inside this packet; no cross-N reading of this packet's outcomes at any stage.
- No overwrite under `results/` or `comparison_bench/outputs_comparison/`; no commit/push; no self-acceptance; no P20S rerun under this packet. No efficiency language anywhere. No text-JSON float dumps of the full-block series; no `*.npz`/`*.npy`/`*.parquet` evidence files.

## 14. Acceptance IDs

- `P20T-R1`: worktree prior arrays verified read-only (canonical digest replay-exact; H literals recomputed within 1e-12; floor pins; zero counts-NPZ opens at every stage) + N=8192 derivation contract verified (worktree-prior-only synthetic TRAIN inputs under frozen seeds/budget, deterministic spike permutation, fresh length-8192 permutations, K2-prefix disclosed, formula-id pin with R=8 re-freeze + program pin + file-bytes digests + set-delta table frozen at Stage A). Derivation-gate BLOCKED → planner rework, never a Stage-B fallback.
- `P20T-R2`: budget/split/orders derived in-packet from recomputed 2M H (K rule display + TRAIN-residual (K1,K2) selection; never from 1.5M, never from N=32768, never from alt-H; zero scoring-time sampling); closed/consumed blocks select nothing; consumed 1M pool + all consumed 1.5M ranges + consumed 2M TRAIN-as-DEV + consumed 2M VAL/HOLD DEV + P20S merged DEV excluded by source-tagged cross-file gates; DEV (HOLD 3595..3626 → ONE N=8192 block, remainder 3627..3644 + 1.5M stubs never used) gated by the exact 32-frame list rule + Pre-EXECUTE-approved; 1M/1.5M/2M-consumed untouched in every form.
- `P20T-R3`: disclosure point preregistered per arm from the in-packet integers (A/B `5·(K1+K2)+64` with Δ exactly 0; O `5·K2+64`; 81983 public; totals derived at Stage A), recount mismatch 0; set-delta recorded byte-exact with size-delta exactly 0; CE ratios never called efficiency; all K/cap values shown as estimates until Stage-A literals freeze them.
- `P20T-R4`: arm pins (A newly-derived frozen-order-equivalent + B spike candidate + shared-L1 rule + O newly-derived-order oracle D-continuity, §3 α1 rule, formula-id + R=8 re-freeze + derivation-program pin) frozen before execution, unchanged after; no tag-guided selection, no evidence reuse, no post-hoc re-picking, no re-derivation.
- `P20T-R5`: L1 / hard-L2 / oracle-L2 / pair endpoints separated per §9; `undetected` isolated; oracle arm never operational; first-error coordinate/layer rows + per-record floor-hit fields + all nine carried scalars (arm-specific order digest) complete per record with correct nullability + IR-1..IR-4 ALL PRESENT with P20Q-identical caps/nullability on every completed record (IR-3 two thresholds 1.0×/2.0× prefix-mean; IR-4 k=16; record's OWN order used for every prefix flag) + IR-5 N=8192-sized (nine `.bin` files + `ir5full-v1` manifest with matching digests on every completed record); instrumentation + IR recorded-only (truth-isolation sentinel green).
- `P20T-R6`: one-shot Tier-Y semantics — single attempt, no rerun/tuning; split-counted reads (counts 0/0 + DEV 1/1 + 1M 0 + 1.5M 0 + 2M-non-DEV 0); derivation sampling = frozen 16-block budget; 5 SC / 3 tags / 3 records derived; resource aborts via P20A path with accounting preserved; stop rules + SCL gate intact.
- `P20T-R7`: independent Pre-EXECUTE + Pre-RESULT reviews recorded (Pre-EXECUTE explicitly adjudicates §3 reuse/derivation contract + formula-id with R=8 re-freeze + TRAIN-seed/budget pins + K-rule derivation + §4 gate family (a)→(g) incl. the single-segment frame-set identity rule + §2 runner design + §7 nine-scalar + IR-1..IR-4 boundary + IR-5 8192-sizing rule + arm-specific order-digest rule + comparability boundary); main-thread acceptance owns the label; within-N geometry/coverage-descriptive only, no recovery-rate/FER/qualification/promotion/H2/cross-N language; honest-scope statement (§0) repeated verbatim in the return.
- `P20T-R8`: Stage-A suites green on injected data with the zero-protected-open audit (counts 0/0 + DEV 0/1 at Stage-A close; 1M/1.5M/2M-consumed non-access in every form); no commit/push; frozen dirs byte-untouched except the §12 manifest.
- `P20T-R9`: derivation-gate (formula-id + R=8 re-freeze + derivation-program pin + fresh-order/spike/construction digests + K literals frozen BEFORE any DEV contact; MISMATCH → DEV untouched, no Stage-B request). Gate-fired → main-thread re-frame, never a Stage-B fallback.

## 15. Return conditions

Per `AGENTS.md` §10.1 exactly two operator return conditions: (a) ALL IDs `P20T-R1..R9` (stage-appropriately) complete with changed files + exact commands/results + artifact inventory (incl. all reuse digest replays + new-construction/fresh-order/spike digests + formula-id with R=8 re-freeze + program pin) + split open audit (counts + DEV + 1M + 1.5M + 2M-non-DEV separately); or (b) a concrete blocker with failing command, exact error/traceback, attempted remedies, and the ONE decision needed from the main thread. "Still incomplete" is not a completion report. The operator never marks its own work accepted and never authorizes Stage B.

## 16. Deferred options (not in this packet)

- Stub/remainder follow-ups: the HOLD remainder 3627..3644 (18 frames) and the 1.5M stubs stay counted never-decoded; no follow-up packet forms an N-block from them without new acquisition or a new N/design (which needs a new OpenSpec change, not a packet).
- 1.5M closure: VAL stub 2172..2212 (41) + HOLD remainder 2725..2766 (42) stay never-decoded; no 1.5M packet follows without new acquisition or a new N/design (which needs a new OpenSpec change, not a packet).
- Operating-point redesign (new α, new floor, SCL-on-real-data, bounded search, second construction): follow-up planning only, never inside this packet; trigger = main-thread reading of accepted within-N P20T tables, never an in-packet verdict.
- New-acquisition ladder: any real-data block beyond this DEV block requires newly acquired frames. Trigger = new frames landed + their own freeze, tag domain, and reviews. Nothing is auto-triggered inside this packet.
- H2 adjudication: EXCLUDED from this packet's downstream (no H2 input); the H2 verdicts stay adjudications over the N=32768 join geometry.
- Cross-N inference: EXCLUDED — per-N results are NOT comparable across N (delta ADDED-7); this packet licenses no scaling, transfer, or promotion reading.
- The DEV block is CONSUMED by this packet regardless of outcome; HOLD remainder 3627..3644 and all 1.5M remainders stay never-decoded. Any follow-up packet needs its own freeze, tag domain, and reviews.

(End of file)
