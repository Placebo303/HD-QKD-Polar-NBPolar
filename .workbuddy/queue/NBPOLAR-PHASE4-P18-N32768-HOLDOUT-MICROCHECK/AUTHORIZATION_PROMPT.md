# Explicit authorization — Phase 4-P18

I authorize Phase 4-P18 exactly as frozen in
`.workbuddy/queue/NBPOLAR-PHASE4-P18-N32768-HOLDOUT-MICROCHECK/TASK_PACKET.md`.

You may implement the scoped OpenSpec delta, thin HOLD microcheck runner and
injected tests; run focused and complete NB-Polar tests; obtain independent
reviewer-go Pre-EXECUTE review; and, only after PASS, execute the frozen
three-block 1M HOLD command once under its resource limits. This authorizes
exactly one V25 TRAIN NPZ content open, one 1M pairs-parquet content open and
one scientific attempt, consumed at the first protected content open.

Use only HOLD frames 1600..1983 as the three registered chronological,
non-overlapping N=32768 blocks. Frames 1984..1999 are an unused remainder.
Do not shuffle, resample, overlap, pad, pool, fit on HOLD or change the P16/P17
construction, K values, decoder, prior, tag, thresholds or accounting. The
run has no recovery/FER threshold; 0..3 exact outcomes are descriptive.

After the run, obtain independent reviewer-go Pre-RESULT review, preserve the
five-file evidence root and report the frozen classification. On any failed
review, identity mismatch, unauthorized access, semantic/resource failure or
existing target root, STOP and return the exact evidence. Do not repair,
rerun, retune, change seeds, clean outputs, commit or push. Finish with memory
triage and return to the main thread for acceptance.
