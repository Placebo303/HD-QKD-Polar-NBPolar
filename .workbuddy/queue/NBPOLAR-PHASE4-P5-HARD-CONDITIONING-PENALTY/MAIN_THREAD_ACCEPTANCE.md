# Main-thread acceptance — Phase 4-P5

Verdict: `HARD_L1_CONDITIONING_PENALTY_ACCEPTED`.

At the frozen strong dependent-L2 synthetic point, all integrity gates pass and
the paired discriminator confirms a material hard-candidate penalty: cells
233/144/0/7, oracle exact 377/384, and the one-sided exact lower bound for the
oracle-only event is 0.3338842736427746 > 0.30. Attempt and streams remain
consumed; the evidence root is immutable.

This rules against directly scaling the tested single-hard-candidate L2
conditioning architecture. It does not establish a real-data penalty, achieved
efficiency, or that APP/SCL is required. Next test the simpler Bob-marginal L2
prior as Tier-X exploration.
