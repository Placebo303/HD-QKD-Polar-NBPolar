# Operator prompt

Execute TASK_PACKET.md exactly. First reconstruct and test the frozen dependent
table and paired exact statistic, then obtain independent Pre-EXECUTE PASS.
Run the single 384-pair attempt once and obtain independent Pre-RESULT review.
Return only the registered confirmed/not-confirmed/blocker outcome with four-cell
counts, exact lower bound, attempt/seed accounting and five-file inventory.
Do not self-accept, tune, rerun, commit or push.
