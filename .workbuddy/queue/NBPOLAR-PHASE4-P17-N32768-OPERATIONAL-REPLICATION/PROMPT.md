Implement and, only after independent reviewer-go Pre-EXECUTE PASS, execute
the complete P17 Tier-Y delta packet in `TASK_PACKET.md`. Reuse the accepted
P16 construction exactly, run only the fresh 128-block operational replication,
and preserve all one-open/one-attempt, checkpoint, accounting, resource and
STOP rules. Obtain independent reviewer-go Pre-RESULT review before return.
Do not commit, push, self-accept or expand scope.

