# Two-step authorization prompts — NBPOLAR-PHASE4-P20T-N8192-PERSISTENCE

Per `AGENTS.md` §10.1 each step below is directly copyable. STEP-1 is copyable
now for the Stage-A freeze/implementation scope (new N=8192 derivation under the
frozen rules: worktree-only inputs, frozen sampling budget/seeds, carried
F-median8 with R=8 re-frozen as a new decision, in-packet K derivation, new tag
domain, zero protected opens). The Stage-B text is a TEMPLATE until Stage A
fills the `<FROZEN_AT_STAGE_A>` pins; the main thread pastes the filled version
only after Stage-A return + independent Pre-EXECUTE PASS + target-output
absence. Neither text authorizes anything beyond its named step.

Standing pre-authorization record (2026-09-20): the user renewed standing
pre-authorization for autonomous推进 with recommend picks, under which this
RN-2 Tier-Y packet freeze proceeds. Stage-A and Stage-B still require their own
freezes + independent reviews + authorizations per the gate sequence — the
standing pre-authorization does NOT collapse Tier-Y gates and does NOT authorize
Stage-B execution, any DEV contact, or any claim.

---

## STEP 1 — Stage-A authorization (copyable now; Stage-A-derived digests filled at freeze)

I authorize Stage A implementation ONLY for
`NBPOLAR-PHASE4-P20T-N8192-PERSISTENCE` exactly as frozen in its
`TASK_PACKET.md` (§§2-7, §12).

This authorization permits: read-only inventory of the packet-§12 predecessor
helpers (including the P20Q `_ir_hazard_diagnostics` code point cited as the
carried-over callsite pattern for the §7 recorder extension, the P20R
`l2_order_position_1p5m` derivation-program pattern reference, and the P20S
`l2_mechanism_probe_2m` mechanism-probe + IR-5 writer pattern reference, no
logic change); ZERO protected opens (counts 0/0 at every stage — the V25 counts
NPZ is never opened/statted/listed; DEV R1 stays 0/1; 1M 0; 1.5M 0; 2M-non-DEV
0 in every form) and verifying the §3 worktree inputs by worktree-file digest
recomputation ONLY (prior canonical digest replay-expected
`b16f52165d9f7ef28884df270f921ce07c2a743f4f4b39c3435838809ae1c587` + H literals
`0.02566204884275839 / 0.8069006731309893 / 0.8325627219737477` recomputed
within 1e-12, never hand-filled + `p_b` cross-check + floor 1e-15 pin + exact
key set; N=32768 order/alt files NEVER enter in any form); confirming the §4
DEV integers against the split-manifest JSON metadata ONLY (HOLD `3595..3626`
→ ONE N=8192 block, 32×256 = 8192; remainder `3627..3644`, 18×256 = 4608,
counted never decoded; 1.5M stubs counted never decoded; no parquet/NPZ
open/stat/listing); freezing the synthetic TRAIN sampling budget (block count)
+ derivation seeds in the freeze supplement (worktree arrays ONLY, zero counts
opens, zero real frames, zero genie); recording the carried F-median8
formula-id with the R=8 vs-8192-geometry RE-FREEZE as a new decision
(rationale + window rule in the supplement); running the §3 P16-pattern
derivation (pooled per-layer risks → worst-first length-8192 orders → `K_total`
via `floor((1.3·8192·H−64)/5)` from recomputed H (≈1760 estimate only until
derived, never hand-filled) → exhaustive `(K1,K2)` by TRAIN residual frozen
before DEV, never carried from (334,6746), never from alt-H) emitting the
construction/allocation artifact + fresh frozen-order-equivalent order file +
fresh spike order file (file-bytes digests + formula-id pin + R=8 re-freeze +
derivation-program pin with sampling budget/seeds + A-vs-B set-delta table with
size-delta exactly 0 + zero-counts attestation); computing the fresh TRAIN-only
feasibility outcome (mismatch ends the packet at Stage A with DEV untouched and
no Stage-B request); a new `FROZEN_N = 8192` runner with ONLY the §2 d1–d9
delta, three hardcoded arms `A_anchor_frozen_order_equivalent` α1 +
newly-derived frozen-order-equivalent at in-packet K /
`B_spike_local_order` α1 + newly-derived spike order at identical in-packet K
(the probed factor, derived ONLY under carried F-median8 with the re-frozen R=8
decision) / `O_true_l1_oracle` newly-derived frozen-order-equivalent true-L1
diagnostic at carried K2 (deployable=false), loading the §3 worktree inputs +
Stage-A-derived files read-only behind the `worktree_prior_input_identity` +
`session_h_recomputed` + `k_derived_in_packet` +
`construction_allocation_identity` + `frozen_order_identity_A` +
`spike_order_identity_B` + `derivation_program_identity` gates, plus cross-file
2M identity, single-segment intra-file containment, consumed-1M / consumed-1.5M
/ consumed-2M (+ P20S merged ranges + N=32768-file) exclusions incl. the
DEV∩build-frames disjointness declaration (S2-ii), and exact-32-frame-list
(3595..3626) + order-position-identity + tag-domain fail-closed gates, with the
§7 nine-scalar recorder (arm-specific order digest) PLUS the mandatory
IR-1..IR-4 recorder (P20Q-identical caps/formulas, all PRESENT bounded
recording-only post-decode with the truth-isolation boundary pinned) PLUS the
IR-5 full-block 8192 writer (per record: 8192 float32-LE hazard series = 32768
B + 8192 uint8 in-prefix flags + 8192 uint8 U-domain flags + `ir5full-v1`
manifest linkage = 48 KiB/record; binary `.bin` + JSON manifest ONLY, no
text-JSON float dumps, no npz/npy/parquet; 3 records ≈ 144 KiB, root ≤ ~1 MB,
every committed file ≤ ~2 MB) plus focused injected tests under a fresh additive
`workspace/p20t/<uuid>/` temp root (fresh disjoint test seeds pinned at
freeze); the Stage-A freeze supplement (worktree digest replay + H/K derivation
display + (K1,K2) + sampling budget/seeds + fresh order/spike digests +
formula-id + R=8 re-freeze + program pin + set-delta table + feasibility outcome
+ population + S2-ii + caps from in-packet K + budgets + tag domain + grep proof
+ IR-3 multipliers + IR-4 k + IR-5 8192 encoding/size pins + module path + all
commands verbatim) + implementation notes. Stage A stages NO OpenSpec edit (the
reduced-N delta is already RN-1 PASS frozen).

This authorization permits the §3 derivation ONLY from worktree prior arrays
under the frozen sampling budget/seeds with carried F-median8 + re-frozen R=8
(the `P20T_STOP_SPIKE_WINDOW_UNREFROZEN` guard fires only if the R=8 re-freeze
decision is absent/contradictory). It further permits ZERO protected counts
opens (counts 0/0 at every stage), ZERO DEV/remainder / VAL-DEV / TRAIN /
HOLD-DEV / P20S-merged content opens or stats, ZERO 1M-pool or any-1.5M-split
open/stat/listing/read in any form, ZERO N=32768 order/alt file use in any form,
ZERO `--n 8192` to any frozen-point N=32768 runner, sampling ONLY under the
frozen worktree-only synthetic TRAIN budget (zero real frames, zero DEV), ZERO
genie calls, ZERO real-data decoder execution, ZERO Stage-B output root, NO
lambda anywhere, NO K carried from (334,6746), recomputed from alt-H, or
hand-filled (≈1760 estimate only until derived), NO derivation on real frames,
NO closed-block / consumed-1M-pool / any-consumed-1.5M / consumed-2M / remainder
tuning or peeking, NO second construction, NO construction sweep, NO bounded
search, NO second order beyond the single spike set, NO fourth arm, NO
disclosure-size change (B−A = 0), NO decoder change, NO H2 verdict and NO H2
input, NO recovery-rate reading, NO cross-N inference or pooling, NO SCL/new
kernel/model/schema, NO text-JSON float dumps, NO npz/npy/parquet evidence
files, NO overwrite under `results/` or `comparison_bench/outputs_comparison/`,
and NO commit or push.

Stage-B execution is NOT authorized by this text. It requires, in order: the
Stage-A return (including worktree digest replay + H/K derivation + (K1,K2) +
sampling budget/seeds + fresh order/spike digests + formula-id + R=8 re-freeze
+ program pin + set-delta table + feasibility outcome + freeze supplement +
implementation notes), an independent Pre-EXECUTE review (PASS, explicitly
adjudicating the §3 worktree-input/H/K-derivation, the construction record, the
formula-id carried text + R=8 re-freeze, the sampling budget/seeds pin, the
feasibility outcome, the §5 in-packet-K caps, the §4 gate family (a)→(g) incl.
the exact 32-frame list rule, the §2 runner-derivation design, and the §7
nine-scalar + IR-1..IR-4 boundary + IR-5 8192 encoding/size rule +
arm-specific order-digest rule), the filled Stage-B authorization below naming
the exact frozen command + worktree digest + fresh order/spike digests +
formula-id + derived K literals, and target-output absence. Stop at the packet
return contract or first concrete blocker.

---

## STEP 2 — Stage-B authorization (TEMPLATE — DO NOT PASTE until Stage A fills `<FROZEN_AT_STAGE_A>`)

I authorize Stage B execution ONLY for
`NBPOLAR-PHASE4-P20T-N8192-PERSISTENCE` with EXACTLY the frozen Stage-B command
from the Stage-A freeze supplement (the `<FROZEN_AT_STAGE_A>` pins filled below;
byte-identical to the module `FROZEN_COMMAND`):

```bash
cd /mnt/d/Code/HD-QKD_Polar_Comparison-nbpolar
export OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 MALLOC_ARENA_MAX=2
ulimit -v 2097152
timeout 1200 <STAGE_A_FROZEN_MODULE> --prior <WORKTREE_PRIOR_PATH> --prior-digest <FROZEN_AT_STAGE_A> --source 2M --floor 1e-15 --n 8192 --k1 <FROZEN_AT_STAGE_A> --k2 <FROZEN_AT_STAGE_A> --construction <FROZEN_AT_STAGE_A> --construction-digest <FROZEN_AT_STAGE_A> --dev-pairs comparison_bench/outputs_comparison/nonbinary_diagnostics/v13r3fresh_pairs_20260816/type2_2M_20260121_183657/pairs.parquet --dev-frames 3595 3626 --block-frames 32 --remainder-frames 3627 3644 --tag-master <FROZEN_AT_STAGE_A> --tag-prefix <FROZEN_AT_STAGE_A> --chunk-rows <FROZEN_AT_STAGE_A> --tag-bits 64 --order-file <FROZEN_AT_STAGE_A> --order-digest <FROZEN_AT_STAGE_A> --spike-order-file <FROZEN_AT_STAGE_A> --spike-order-digest <FROZEN_AT_STAGE_A> --spike-formula <FROZEN_AT_STAGE_A> --out-dir .workbuddy/queue/NBPOLAR-PHASE4-P20T-N8192-PERSISTENCE/n8192_tail_persistence_probe
```

with worktree prior digest `<FROZEN_AT_STAGE_A>` replayed, fresh
frozen-order-equivalent digest `<FROZEN_AT_STAGE_A>` (Stage-A derived, pinned in
the freeze supplement), fresh spike digest `<FROZEN_AT_STAGE_A>` (Stage-A
derived, pinned in the freeze supplement), derivation-program pin + carried
formula-id + R=8 re-freeze `<FROZEN_AT_STAGE_A>` (pinned in the freeze
supplement), sampling budget/seeds `<FROZEN_AT_STAGE_A>` (pinned in the freeze
supplement), in-packet K literals `<FROZEN_AT_STAGE_A>` (Stage-A derived from
recomputed H, never hand-filled), new tag master/prefix
`<FROZEN_AT_STAGE_A>`, and DEV R1 `3595..3626` + remainder `3627..3644` — all
pinned in the freeze supplement and adjudicated PASS by the independent
Pre-EXECUTE review.

This authorization permits EXACTLY ONE attempt (attempts 0/1 → 1/1, consumed at
the first DEV R1 content open) producing ONLY the fifteen frozen files under
`.workbuddy/queue/NBPOLAR-PHASE4-P20T-N8192-PERSISTENCE/n8192_tail_persistence_probe/`
(which must be ABSENT now), with DEV reads 0/1 → 1/1, counts staying 0/0 (no
counts open at any stage), 1M 0, 1.5M 0, 2M-non-DEV 0 in every form (remainder +
1.5M stubs counted never-decoded). It permits no rerun, no prior/construction/
K/order/IR-threshold change, no second attempt for any reason except a recorded
identical-freeze repeat of an execution error (never tuning), no 1M-pool or
any-1.5M access in any form, no consumed-2M access beyond frozen DEV R1, no
N=32768 file use, no recovery-rate reading, no overwrite under `results/` or
`comparison_bench/outputs_comparison/`, no H2 verdict and no H2 input, no
cross-N inference or pooling, and no commit or push. The operator returns under
the packet §15 contract and never marks its own work accepted.

(End of file)
