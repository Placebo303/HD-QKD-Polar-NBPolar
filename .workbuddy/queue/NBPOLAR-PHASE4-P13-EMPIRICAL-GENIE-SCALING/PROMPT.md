Implement and, only after independent reviewer-go Pre-EXECUTE PASS, execute the
complete P13 Tier-Y packet in `TASK_PACKET.md`. Preserve all frozen inputs,
seeds, thresholds, budgets, checkpoint semantics and STOP rules. Return only
after independent reviewer-go Pre-RESULT review with the required evidence;
do not commit or push, and do not authorize or accept your own result.

