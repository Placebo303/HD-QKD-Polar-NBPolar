# Phase 4-P1 prompt — prepared, not authorized

Do not execute until `STATUS.yaml` in this directory explicitly authorizes
Phase 4-P1 (all flags are currently false).

When authorized, work autonomously from
`.workbuddy/queue/NBPOLAR-PHASE4-P1-PRIOR-ADAPTER/TASK_PACKET.md` under the
frozen contract `docs/nbpolar/PHASE4_P0_PRIOR_CONTRACT.md` and
`openspec/changes/formal-ir-nbpolar-phase4-p0/`.

Implement only synthetic fixtures + the independent formula oracle
(V-P0-01–07). Do not read CAL, freeze DEV/EVAL identities, run Model-F or a
decoder, or select a floor from data. Never call the banned per-cell twin and
never expose an operational oracle helper. Prepare an inactive Phase 4-P2
CAL/DEV packet, then return one reviewed implementation candidate or one exact
blocker.
