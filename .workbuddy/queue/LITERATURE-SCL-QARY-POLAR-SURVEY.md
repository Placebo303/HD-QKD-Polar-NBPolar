# Literature Survey: SCL for q-ary Polar Codes (bounded, research-only)

- Status: survey only — NO implementation, NO execution, NO repo writes besides this file, NO commit/push.
- Retrieval method: `sciverse` skill only (bundled semantic_search / search_papers / read_content). No web claims.
- Corpus note: Sciverse full-text corpus is AI-conference-heavy; information-theoretic coverage is thin. Findings below are reported honestly with exact coverage evidence; §6 documents gaps.
- Every paper claim carries (doc_id + title + retrieval offset). Offsets are Unicode code-point offsets returned by semantic_search/read_content.
- Relevance verdicts: SUPPORTS / CONTRADICTS / CONTEXT, relative to (i) our SCL-unlock gate (true-path top-L survival at frozen-hazard spike positions), (ii) our spike-local order direction, (iii) the RN N=8192 probe design.
- Bounded to 10 papers, peer-reviewed/preprint only.

---

## 1. Evidence table

| # | Claim | Paper | doc_id | Retrieval offset | Verdict |
|---|-------|-------|--------|------------------|---------|
| 1 | Q-ary 2×2-kernel polar codes over GF(256): pruned-tree SCL matches conventional SCL FER with much lower complexity; at SNR ≥ 2.25 dB average visited nodes converge to SC (N_node,avg ≈ n_c). | Construction and decoding algorithms for polar codes based on 2×2 non-binary kernels (Yuan & Steiner, arXiv 2018) | `d437c2dda0d645ba4cb66f1d8a367757ca1e9ee63d6ce63d1ef95f5ff20902a0` | chunk offset 21384 (read_content p.4) | SUPPORTS (i): reliability-pruned list keeps true path while cutting splits; CONTEXT (ii): pruning rule is SPM-ratio-based (δ1/δ2 thresholds), not spike/hazard-local |
| 2 | Same paper: GF(256) polar codes achieve the same FER as binary 5G polar codes of equal (n_c, k) with the list size reduced by a factor of 4; GF(256) also has better SC (L=1) performance / distance properties than binary. | Yuan & Steiner 2018 (Fig. 7/8 discussion) | `d437c2dda0d645ba4cb66f1d8a367757ca1e9ee63d6ce63d1ef95f5ff20902a0` | read_content offset 19000–22933 (same passage) | SUPPORTS (i): required L shrinks as q grows (near-ML at smaller L); CONTEXT (iii): tested regime is short/moderate n_c on biAWGN, not N=8192 HD-QKD data |
| 3 | Hybrid non-binary repeated polar codes, N=8192, k=40, AWGN: GF(16) with L=8 ≈ GF(4) with L=32 ≈ plain polar-repetition with L=128; GF(16) with L=4 ≈ repetition with L=128. I.e. ~4× list-size reduction per field upgrade (GF(4)→GF(16)), ~32× vs binary repetition. | Hybrid non-binary repeated polar codes (Abbasi, Mahdavifar, Viterbo, arXiv 2021/2022) | `5ab920026a9c48f76a46d0150eb36b771579d500de9a23d7f80d667ed84c5edb` | chunk offset 37227 (read_content 34000–42000) | SUPPORTS (i)+(iii): only retrieved N=8192 q-ary SCL L-scaling point; low-rate (k=40/80) regime matches our short-block probe scale |
| 4 | Same paper: at N=8192, k=80, Eb/N0=1.5 dB, the GF(16)-vs-repetition L-gap grows with L; FER=8×10⁻⁴ reached at L≥64 (GF(4)) vs L>8 (GF(16)). Complexity crossover: GF(4) beats repetition at equal complexity for FER ≤ 6×10⁻³ (L>4 vs L>8); GF(16) crosses at FER=2×10⁻³ (L≥4 vs L≥64). | Abbasi et al. (Figs. 5–7 discussion) | `5ab920026a9c48f76a46d0150eb36b771579d500de9a23d7f80d667ed84c5edb` | read_content 34000–42000 (same span) | CONTEXT (iii): gives an external FER-vs-L slope reference for the RN N=8192 probe, but on AWGN/low-rate, not our channel |
| 5 | Short-message NB polar (256 symbols): BLER curves at L=32/100/625 — 4-PSK binary best at L=32 and L=100; GF(3) only slightly better for Eb/N0>0.5 dB at L=625; GF(5) considerably worse, though its gain from larger L is steeper (all three might converge at some very large L). Decoding complexity grows with q, so only GF(3) retained as alternative. | On channel codes for short underwater messages (Falk, Bauch, Nissen, Information 2020) | `9ec23f2a6017ff8681ba8a8e494846ee912f9b3e8db67066fc36e35ec06f5d6c` | chunk offset 27507; read_content 25000–31000 | CONTRADICTS-in-part (i): larger q does NOT universally reduce required L — GF(5) needs far larger L than binary for parity; SUPPORTS (iii): short-block q-ary SCL reference with explicit L ladder (32/100/625) |
| 6 | Non-binary polar (GF(q=2^p)) + CCSK evaluated under SC only; construction-stage WER estimate is tight vs Monte Carlo; gap to finite-blocklength normal approximation is ~1–1.5 dB; authors state performance "may be further improved by using a more powerful SC-List decoder [8]" — i.e. SCL numbers are future work, not reported. | Non-binary polar codes for spread-spectrum modulations (Savin, ISTC 2021) | `282fc90d39f645d3622496460b539e43f8de40086528c5830b457672fc6293a9` | read_content offset 20000–25067 (Sec. V–VI) | CONTEXT (i): mirrors our own SC-only position — SC characterized, SCL-unlock open; CONTEXT (iii): SC-only WER methodology (estimate-then-simulate) is a template for the RN probe baseline |
| 7 | Split-reduced SCL (binary): skip path splitting when the unfrozen bit is sufficiently reliable; conjecture — if the correct path survives to some stage it tends to survive to termination without further splitting, while incorrect paths keep splitting; a per-path no-split counter prunes paths above/below a threshold at near-SCL FER. Proves existence of a special unfrozen bit u_{N-K1+1} after which SC=ML given correct prefix. | A split-reduced successive cancellation list decoder for polar codes (Zhang et al., IEEE JSAC 2016) | `d89f3014ed91fd0fc2576ceaf79ae086b9a742d8006881d9cb66465db37b3543` | chunk offset 3623; read_content 3000–8000 | SUPPORTS (i)+(ii): direct precedent for "true path survives without splitting once past the hazard" + reliability-gated splitting; the u_{N-K1+1} result is the closest published analogue of our spike-local order argument |
| 8 | Layered critical-set bit-flipping (binary): a constructed critical set S contains the first SC error position with probability typically >99%; progressive multi-level flipping over layered critical sets reaches CA-SCL L=32 BLER at level 4, at ~SC average complexity at medium/high SNR. | Progressive bit-flipping decoding of polar codes over layered critical sets (Chen et al., arXiv 2017; same work: IEEE GLOBECOM 2017) | `c1f687ddb4b4d4554ca57d3e26d9a736cc04c5f36523181a1dab6fc26c2c9406` / `e481f92d9846e111ba3ee76230285e663522949bb160501ed476d03fea13b4c1` | chunk offsets 693 / 677 (abstract-level, both records) | SUPPORTS (ii): published proof that first-error mass concentrates in a small identifiable index set — same structural bet as our spike-local order; CONTEXT (i): flip-based, not list-survival, so the L-mapping (level-4 ≈ L=32) is analogical, not direct |
| 9 | IPSS-SCL (binary): selective splitting via Gaussian-approximation criterion + path-contraction by correct-path location estimation; explicitly adopts the critical set (which "contains more than 99% indices of channel noise erroneous bits") into CA-SCL so splitting happens only at noise-erroneous positions — large average-complexity cut at ~no FER loss on AWGN. | An improved path splitting strategy on successive cancellation list decoder for polar codes (Peng, Bao, Liu, IET Commun. 2021) | `4e6ae76ef0f3fa628e59082e75b701de178886d2df65682388abe98620af69af` | chunk offset 12578 | SUPPORTS (ii): critical-set-gated splitting inside SCL is published and works; CONTEXT (i): gates are GA-reliability-based, not empirical spike/hazard-based — our hazard mass (IR-4 top-16 in-prefix 0/48) has no published counterpart |
| 10 | HD-QKD information reconciliation uses nonbinary LDPC + Cascade, achieving efficiencies f=1.078–1.14 (d=8, QBER 3–15%) near the Slepian–Wolf bound on q-ary symmetric channels; NB-LDPC order matched to qudit dimension; NO polar code (binary or q-ary) appears in either method. | Efficient information reconciliation for high-dimensional quantum key distribution (Müller et al., Quantum Inf. Process. 2024) | `bcb3e9cf6ea2c174dfee7ef7fe82c22231063082e2c5ce4c8126fbf28158b216` | chunk offsets 649 (abstract), 36909, 46402 | CONTEXT (Q4 + iii): field baseline for HD-QKD IR is NB-LDPC/Cascade on the q-ary symmetric channel — our NB-Polar track has no published HD-QKD competitor/collaborator; also frames the adversarial null (no one has shown polar+SCL failing here either) |

### Companion metadata-level items (abstract evidence only, not counted in the 10)

- A. Feng et al., "Simplified Successive-Cancellation List Decoding of Non-Binary Polar Codes with Rate-1 Node" (IEEE WCNC 2020), unique_id `paper:10.1109/wcnc45663.2020.9120673`, doc_id `6f6069c72f9efaf59ba113c575a59fd2c988a4a0dd19e587dd98835b4da88753`: LLR path metric for NB-SCL + unreliable-symbol selection inside Rate-1 nodes so paths split only on selected symbols — no performance loss (abstract from search_papers; full-text fetch returned FETCH_FAILED, so not a primary citation). Verdict: SUPPORTS (ii), abstract-level.
- B. Li et al., "Research on encoding and decoding of non-binary polar codes over GF(2^m)" (Digital Commun. Networks 2022), doc_id `e1bf7a0cd9b64476b01147d88ad11c5386a5046c3698ea7053ac74aa8d15b14e`: LLR-based NB-SCL for arbitrary-size kernels + Perfect-Polarization-Based SCL skipping splits at highly reliable positions, ~40% complexity cut, NB BER "significantly outperforms" binary (abstract from search_papers). Verdict: SUPPORTS (i)+(ii), abstract-level.

---

## 2. Per-question findings (one line each)

- **Q1 (L for near-ML in q-ary SCL; scaling with N/rate):** Required L shrinks with q — GF(256) matches binary FER at L/4 (Yuan & Steiner 2018, doc `d437c2dd…`, off. 21384); at N=8192/k=40, GF(16)/L=8 ≈ GF(4)/L=32 ≈ binary-repetition/L=128, i.e. ~4× per field upgrade (Abbasi et al., doc `5ab92002…`, off. 37227); but the trend is NOT monotone — GF(5) needs far larger L than binary GF(3)/4-PSK parity (Falk et al. 2020, doc `9ec23f2a…`, off. 27507). No retrieved paper gives an (N, rate)→L scaling law; all points are isolated simulations.
- **Q2 (reliability/hazard/spike-aware selection, order, pruning):** Three published mechanisms directly parallel our direction — reliability-gated split skipping with correct-path survival conjecture + post-u_{N-K1+1} SC=ML theorem (Zhang et al. 2016, doc `d89f3014e…`, off. 3623); >99%-coverage critical sets of first-error positions used for flipping (Chen et al. 2017, docs `c1f687dd…`/`e481f92d…`, off. ~690) and imported into CA-SCL splitting (Peng et al. 2021, doc `4e6ae76e…`, off. 12578); SPM-ratio pruned-tree NB-SCL collapsing to SC at high SNR (Yuan & Steiner 2018, doc `d437c2dd…`, off. 21384). None is spike/hazard-local in our sense (empirical frozen-hazard mass); all gates are GA/Bhattacharyya/LLR-reliability-based.
- **Q3 (N≈8192 q-ary SC/SCL external reference):** Exactly one retrieved N=8192 q-ary SCL point family exists — Abbasi et al. hybrid repeated codes (N=8192, k=40 SC + SCL; k=80 FER-vs-L at 1.5 dB with FER 6×10⁻³/2×10⁻³/8×10⁻⁴ crossover ladder, doc `5ab92002…`, off. 37227, AWGN, very-low-rate); nearest neighbors are short-block (256-symbol) GF(3)/GF(5) BLER-vs-L ladders (Falk et al. 2020) and an SC-only GF(2^p)+CCSK study whose authors defer SCL to future work (Savin 2021, doc `282fc90d…`, off. 20000+). Nothing at N=8192 on a QKD-like or heavy-tail channel.
- **Q4 (adversarial — list does NOT help q-ary polar on heavy-tail/zero-count channels):** No retrieved paper states or shows this; the nearest negatives are (a) Falk et al. GF(5)-much-worse-than-binary at fixed L (a q-mismatch penalty, not a channel-tail effect), and (b) the HD-QKD IR literature standardizing on NB-LDPC/Cascade with zero polar presence (Müller et al. 2024, doc `bcb3e9cf…`, off. 649/36909/46402) — consistent with "SCL-unlock on real HD-QKD data is unproven", but it is absence of evidence, not evidence of absence.

---

## 3. Concrete (N, K, L, FER/channel) points extracted

| Source | N | K / rate | L | Result (channel) |
|--------|---|----------|---|------------------|
| Yuan & Steiner 2018 (Fig. 8) | moderate n_c (binary-equivalent length; exact N in figure, not text) | matched (n_c, k) + outer code | NB-GF(256) L vs binary 4L | equal FER (biAWGN); NB SC better than binary SC (Fig. 7) |
| Abbasi et al. (r=16, CRC-SCL) | 8192 | k=40 | GF(16)/L=8 ≈ GF(4)/L=32 ≈ repetition/L=128; GF(16)/L=4 ≈ repetition/L=128 | FER curves (AWGN); r=32/64 same ordering, [7] unbeatable only at r=64 |
| Abbasi et al. (r=16, k=80, 1.5 dB) | 8192 | k=80 | FER 8×10⁻⁴ at L>8 (GF16) vs L≥64 (GF4); complexity crossover FER 2×10⁻³ (L≥4 vs L≥64) | AWGN |
| Falk et al. 2020 (Fig. 8) | 256 symbols | ~0.5 bit/ch.use (K′=80/55/46 for q=3/5/7 + GF(q) CRC) | 32 / 100 / 625 | BLER (AWGN+PSK): binary 4-PSK best at L≤100; GF(3) edges ahead only at L=625, Eb/N0>0.5 dB; GF(5) far worse |
| Savin 2021 (Fig. 4/5) | N=2^n symbols, Np-bit equivalents (Table I) | R=1/48–9/10 | L=1 (SC) only | WER (CCSK+real AWGN, −25…−5 dB); 1–1.5 dB from normal approximation at WER 10⁻⁴ |
| Chen et al. 2017 | finite-length binary (code params in paper body) | — | level-4 flip ≈ CA-SCL L=32 | BLER (AWGN); >99% first-error coverage by critical set |

---

## 4. What this means for our three reference points

- **(i) SCL-unlock gate (true-path top-L survival at spikes):** Literature says the gate is well-posed and usually passable — correct-path survival without splitting is the normal case past reliable positions (Zhang 2016), critical sets cover first errors at >99% (Chen 2017), and q-ary lists need *less* L, not more (Yuan 2018; Abbasi). Nothing retrieved suggests a structural reason the true path must die at our spikes; but no paper tests survival under zero-count-dominated posteriors either, so our X14-style measurement remains the only direct evidence and must stay the gate.
- **(ii) Spike-local order direction:** Supported as a family (reliability-gated splitting/pruning is a published, winning idea in 4+ variants), but our specific gate signal — empirical frozen-hazard spike mass with IR-4 top-16 in-prefix 0/48 — has no published analogue; all published gates use analytic reliability (GA/Bhattacharyya/SPM-ratio/LLR). Keep the direction; do not claim literature precedent for the hazard signal itself.
- **(iii) RN N=8192 probe design:** Abbasi N=8192/k=40–80 gives the only external L-ladder template (expect steep FER-vs-L slope differences between fields; plan L∈{4,8,32} minimum to resolve GF(32) vs baselines) and Savin gives the SC-baseline-first methodology; Falk warns that bigger-q can *lose* at fixed L, so the probe must include a binary/CRC-SCL control at matched L, not just q-ary internal comparison. **[Adjudicated 2026-09-20 — NOT applicable to RN (frozen SC-only scope; no SCL module exists; SCL locked). Deferred to a future separately-gated SCL track; L values must be re-justified then, never inherited. See §7.]**

---

## 5. Bottom line for main-thread planning (5 lines)

1. Unlocking SCL is the literature-consistent move: q-ary SCL typically needs *smaller* L than binary (4× per field upgrade in two independent studies), so a GF(32) list should be cheap *if* our channel behaves like tested ones.
2. Spike-local reliability gating is a published winning family (split-reduced, critical-set, pruned-tree, PPB-SCL), backing our order-direction — but our frozen-hazard signal itself is novel, so keep X14-style survival as the hard gate.
3. The only external N=8192 q-ary SCL reference (Abbasi: GF16/L=8 ≈ GF4/L=32 ≈ binary/L=128) sets the RN probe's L-ladder (include 4/8/32 + binary control); expect field-dependent slope, not just offset. **[Adjudicated 2026-09-20 — rejected for RN for the same reason; kept as context for future SCL planning only. See §7.]**
4. Adversarial case is weak: no paper shows lists failing on heavy-tail/zero-count channels; the real risk is mundane — GF(5)-style q-mismatch penalty (Falk) and the field's revealed preference for NB-LDPC/Cascade in HD-QKD IR (Müller).
5. Hard gaps bound what this survey can bless: zero GF(32)-polar-SCL points, zero N≥8192 q-ary points outside AWGN/low-rate, zero heavy-tail/QKD-channel SCL studies — the RN N=8192 block is genuinely uncharted, so treat its outcome as discovery, not confirmation.

---

## 6. Coverage gaps (explicit)

1. **GF(32) / q=32 polar SCL: zero points.** Structured search `search_papers(query="GF(32) polar codes", page_size=10)` returned 10 results whose top hits are GF(3)/GF(5) short-message studies (Falk/VTC2020, unique_id `paper:10.1109/vtc2020-fall49728.2020.9348796`), a general GF(2^m) paper (Li et al. 2022, `paper:10.1016/j.dcan.2022.01.005`), and unrelated non-polar GF(32) LDPC-for-CV-QKD work — no GF(32) polar SCL simulation anywhere in the retrieved set.
2. **(N, rate)→L scaling law: absent.** All retrieved L-evidence is isolated (N, K, L, FER) points (Yuan Fig. 7/8; Abbasi Figs. 4–7; Falk Fig. 8); no paper derives or fits how required L scales with N or rate for q-ary SCL.
3. **Heavy-tail / zero-count / QKD-operating-point channels: absent.** Every retrieved SCL performance point is on AWGN (mostly biAWGN/BPSK or CCSK-AWGN) or erasure channels; QKD semantic searches return channel-capacity studies (Brougham et al., EPJD 2016, doc `1076b1fa05…`, off. 56900) and IR method papers (Müller 2024; Pacher et al. CV-QKD NB-LDPC GF(8–64), doc `ccb756a3cb…`, off. 45443) — none combines polar SCL with a spiky/zero-inflated channel.
4. **N≈8192 q-ary at operational rates: absent.** The single N=8192 q-ary SCL study (Abbasi) is very-low-rate (k=40/80, repetition-aided, low-SNR/AWGN); nothing at N=8192 with moderate-rate q-ary SCL.
5. **Frozen-hazard / spike-local phenomena: absent.** No retrieved paper discusses zero-count-dominated posteriors, frozen-position hazard spikes, or in-prefix survival statistics; the closest concepts (critical sets, vulnerable-bit sets in Rowshan et al. 2019, doc `84543f7a08…`, off. 4168) are analytic-reliability constructions on AWGN.
6. **Corpus bias note:** Sciverse full-text coverage skews to recent AI-conference/checkable content; several authoritative polar works appear only as metadata/abstracts (e.g. Feng WCNC 2020 full-text fetch failed with FETCH_FAILED; Li DCAN 2022 abstract-only). Two companion items above are honestly marked abstract-level and excluded from the 10-paper count.

---

## 7. Main-thread adjudication (2026-09-20)

- The L-ladder recommendation in §4(iii) and §5 item 3 (L∈{4,8,32} + binary/CRC-SCL control) is REJECTED for the RN N=8192 probe: frozen SC-only scope, no SCL module exists in `formal_ir/nbpolar/`, SCL stays locked; an SCL arm would be a second factor plus a new decoder. Original survey text above is kept for provenance; the inline tags mark the adjudication.
- DEFERRED to a future separately-gated SCL track (own OpenSpec change + freeze + authorization). Rule for that track: first establish a synthetic working point that distinguishes SC vs list gains; only then decide an L-ladder, with L values re-justified from that working point — never auto-inherited from this survey.
- No other survey content is altered by this adjudication.
