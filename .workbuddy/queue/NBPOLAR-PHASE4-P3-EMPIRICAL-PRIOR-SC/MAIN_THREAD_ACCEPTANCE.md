# Main-thread acceptance — Phase 4-P3

Disposition: `EMPIRICAL_PRIOR_SC_INTERFACE_ACCEPTED`.

The main thread accepts the reviewed candidate as evidence that the accepted
CAL-derived P1 metric is numerically consistent with the accepted q-ary SC
decoder and an independent exhaustive oracle under model-sampled data.

Accepted evidence is limited to the single consumed Stage B attempt: 328
executed blocks, 48 exact, 280 non-exact, zero nonfinite/resource-abort/truth
leak, all six hard gates true, B1 maximum probability/log errors
`1.11e-15`/`5.33e-15`, and independent Pre-EXECUTE `PASS` plus Pre-RESULT
`PASS_WITH_COMMENTS`. The five-file evidence root and seed `2026091316` are
consumed and immutable. No rerun is allowed.

This acceptance is not FER, reconciliation, leakage, key-rate, construction/K
or performance evidence. The B2/B3 exact counts do not promote a disclosure
pattern. It authorizes no Phase 5 implementation or execution. Phase 5 is
represented only by a separately frozen, all-false task packet requiring an
explicit user grant.
