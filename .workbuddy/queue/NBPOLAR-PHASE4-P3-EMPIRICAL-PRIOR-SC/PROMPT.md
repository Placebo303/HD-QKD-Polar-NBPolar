# Phase 4-P3 prompt — prepared, not authorized

After explicit authorization, run the complete heavy P3 packet autonomously.
Before artifact access, compare vectorized and literal likelihood paths, test
probability/log representations, exercise zero-support and extreme-dynamic-range
cases, run transform/interface metamorphic checks, validate failure taxonomy,
and profile N=2..1024 where resource limits permit. Fix routine in-scope defects
and keep exploring until every Stage A item is resolved. Then freeze seeds,
masks, Stage B command and target, obtain independent Pre-EXECUTE PASS, perform
one artifact-content read, and complete the tiny oracle, disclosure-shape,
N=256, resource and attribution matrices from the in-memory table. Finish with
independent Pre-RESULT review. Never use raw/held-out data, tune construction/K,
enter L2/protocol or claim FER.
