# Phase 4-P2 prompt — prepared, not authorized

Do not execute until the user sends `AUTHORIZATION_PROMPT.md` and STATUS
reflects exactly that grant. Then work autonomously from `TASK_PACKET.md`.

Qualify the loader on synthetic fixtures first. Obtain real independent P2-G0
PASS before reading the sibling artifact. Perform at most one content-read
attempt, write only the two packet-local summaries, obtain independent
Pre-RESULT review, and return one candidate or exact blocker. Never run SC,
Model-F fitting, raw data, DEV/EVAL, benchmark or reconciliation.
