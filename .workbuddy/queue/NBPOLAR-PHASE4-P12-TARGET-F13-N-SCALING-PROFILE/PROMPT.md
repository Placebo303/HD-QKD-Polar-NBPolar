Implement and execute the authorized Tier-Y P12 task exactly as frozen in
`TASK_PACKET.md`. Add OpenSpec first; preserve accepted support, chunked SC,
two-layer and tag semantics. Run focused and predecessor tests, freeze the
command, and obtain independent reviewer-go Pre-EXECUTE PASS before the sole
NPZ open, which consumes read/attempt 1/1. Execute the six-N/128-block profile
once without tuning or rerun, then obtain Pre-RESULT review. Recovery has no
pass threshold. Return complete candidate evidence or a concrete blocker; no
commit/push.
