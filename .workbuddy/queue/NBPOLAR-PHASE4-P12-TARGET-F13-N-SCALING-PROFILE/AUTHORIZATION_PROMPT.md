I authorize the complete Tier-Y task in `.workbuddy/queue/NBPOLAR-PHASE4-P12-TARGET-F13-N-SCALING-PROFILE/TASK_PACKET.md`: add the P12 OpenSpec delta; implement the thin target-population f=1.3 N-scaling runner and focused injected tests while preserving accepted support, chunked SC, two-layer hard-candidate and tag/accounting semantics; run the accepted 262-test predecessor suite; freeze the exact command/root/allocation/output contract and obtain independent reviewer-go Pre-EXECUTE PASS; then open the registered V25 1M TRAIN counts exactly once, consuming read 1/1 and attempt 1/1, and execute the frozen six-N/128-block profile once with seeds 2026091820..1825, N-specific BEC orders, deterministic full-budget K allocation, chunk_rows=512 and the 1800-second/2-GiB envelope. Obtain independent Pre-RESULT review before return. Recovery is descriptive with no performance threshold. No reopen, rerun, tuning, frozen-value change, Model-F/raw/held-out/real/EVAL access, empirical large-N learning, adaptive work, FWHT/APP/SCL, qualification/promotion, old-root modification, commit or push. STOP and preserve evidence on any blocker or integrity/resource failure.
