# Operator prompt — Phase 6 fixed incremental disclosure

Read `AGENTS.md`, `docs/nbpolar/WORKBUDDY_LIFECYCLE.md`, Phase 5
`MAIN_THREAD_ACCEPTANCE.md`, the Phase 6 OpenSpec, and this directory's
`TASK_PACKET.md` and `STATUS.yaml`. Do nothing unless the exact authorization
has been supplied. Complete the full packet or return one earliest blocker; do
not stop at the first working example.
