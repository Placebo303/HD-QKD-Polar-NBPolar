Implement and execute `.workbuddy/queue/NBPOLAR-PHASE4-P8-TARGET-RATE-SCREEN-CONFIRM/TASK_PACKET.md` exactly. Add the P8 OpenSpec delta first, reuse the accepted P7 orders/support/protocol, and implement the frozen shared-block SCREEN plus deterministic selection and disjoint CONFIRM. Run focused and predecessor tests; obtain independent reviewer-go Pre-EXECUTE PASS; execute the single frozen command with one artifact read/attempt; then obtain independent Pre-RESULT review. Return only the frozen label or a concrete blocker. No rerun/tuning, held-out/real data, adaptive/N>256/FWHT/APP/SCL, commit or push.

