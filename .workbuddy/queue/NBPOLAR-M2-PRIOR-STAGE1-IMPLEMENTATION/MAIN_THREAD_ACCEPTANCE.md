# M2 Stage-1 main-thread acceptance (2026-09-21)

Label: `NBPOLAR_M2_PRIOR_STAGE1_IMPLEMENTATION_COMPLETE_ACCEPTED`.

1. Acceptance basis: user authorization pasted verbatim (Stage-1 implementation only; execution_authorized false) + operator all-complete return (S1-1..S1-8 with evidence paths, 21-green focused suite ~8 s, `--help` exit 0, `--selfcheck` 12/12 PASS, scoped 4-file status) + independent focused review PASS_WITH_COMMENTS (`REVIEW.md`; per-check A–H all PASS; zero blocking issues; 4 operator-reported concerns adjudicated ACCEPTABLE). No Pre-EXECUTE/Pre-RESULT gates apply: this packet is decoder-free, synthetic-fixture-only, zero protected reads, zero data contact — no claim-bearing execution.
2. Delivered surface (additive only): NEW `comparison_bench/src/comparison_bench/formal_ir/prior_m2.py` (strict M2 ±1 adapter beside — never inside — `formal_ir/nbpolar/`; numpy+stdlib; zero `nbpolar/` imports; FLOOR=1e-15 literal; MOD frozen LINEAR_ONLY default; M0 switch reproduces the P7 raw-MLE-plus-floor rule bit-exact `diff==0.0`); NEW `comparison_bench/tests/test_nbpolar_prior_m2.py` (T1–T12); runner `scripts/m2_prior_validation.py` upgraded skeleton → Spec 5 (all 19 TO-FREEZE keys enforced with null/absent ⇒ exit 2, never a default; `--authorized` store_true; flag↔key cross-check; K pinned 319/6492; `--out-root` confined to `workspace/`; post-validation bodies exit 3 `STAGE_BODY_PENDING_FREEZE` with zero data contact); `docs/SECURITY_MODEL.md` +39 lines (32-frame CAL sacrificed + excluded from denominator; reveal ~18–22 bits diagnostic-only, never a λ_prior term; claim scope unchanged; M2 labelled CANDIDATE; 0-bit inventory skeleton).
3. Scientific status unchanged by this packet: M2 remains a CANDIDATE pending real-data validation. S9 stays synthetic-only (16 blocks, pro-M2 ground truth by design) and is never FER/efficiency evidence. No K decision was made (fixed-K vs fixed-f still DEFERRED); no freeze value was set; G1/G2/G3 remain unauthorized. Frozen `nbpolar/` package, `src/`, `experiments/`, `tools/`, `results/`, `comparison_bench/outputs_comparison/` untouched (reviewer-verified empty diffs).
4. Carried non-blocking items (from review; no rework required): cosmetic rename `t_stubs_raise_not_implemented` → `t_stage_bodies_exit3`; add a one-line note that the `_out_root_refusal` forbidden-tree guard is additive to Spec-5 `workspace/` confinement. Pre-existing tree dirt (`.codebuddy/memory/*`, csv stat-dirty, `.workbuddy/memory/`) is outside this packet and was deliberately untouched.
5. Next gate: Stage-2 G1 freeze preparation (19 TO-FREEZE values incl. W_P=500 DATA-INFORMED / W_S=200 / skip=702 / MOD=LINEAR_ONLY / NCHAR≥200k / B_tail=2.0e-4 / Δ_min=0.020 / CAL-split RULE-FIT32-SCORE-HELDOUT / g2_blocks=16 / tag_master frozen at freeze time), gated on independent freeze review + explicit user authorization; G2 (Tier-Y one-shot decode at frozen K1=319/K2=6492 on SHG `_1`) remains a separate later freeze. Milestone-batch ledger updates (decision-log / memory / index) are main-thread work, not in-packet.
