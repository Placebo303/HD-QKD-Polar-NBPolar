# Execution-session prompt

Work in the current repository root on branch `codex/nbpolar-phase0`.

Execute the complete frozen packet:
`.workbuddy/queue/NBPOLAR-PHASE1-GF32-TRANSFORM/TASK_PACKET.md`.

Read the packet and every required source document completely before editing.
You own only the packet allowlist. Implement Phase 1 GF4/GF32 field adaptation,
the frozen natural-order row-vector NB-Polar transform, an independent
reference transform, source coordinate selection, and the complete focused
T0/T1 test matrix. Preserve unrelated dirty changes.

The user has authorized this packet's implementation and focused synthetic
tests. No SC decoder, Model-F/CAL/VAL/TTBin, real data, result root, benchmark,
commit, push, qualification, or scientific conclusion is authorized. Do not
advance to Phase 2.

Return only `COMPLETE` with `OPERATOR_RETURN.md` and
`STATUS.yaml=IMPLEMENTATION_CANDIDATE`, or `BLOCKED` using the packet's exact
blocked format. You cannot accept your own implementation.
