# Operator prompt — Phase 5 static protocol

Read `AGENTS.md`, `docs/nbpolar/WORKBUDDY_LIFECYCLE.md`, the Phase 4-P3
`MAIN_THREAD_ACCEPTANCE.md`, and this directory's `TASK_PACKET.md` and
`STATUS.yaml`. Do nothing unless the user has supplied the exact authorization
from `AUTHORIZATION_PROMPT.md`. Execute the complete packet or return the one
earliest blocker; do not stop at the first working example.
