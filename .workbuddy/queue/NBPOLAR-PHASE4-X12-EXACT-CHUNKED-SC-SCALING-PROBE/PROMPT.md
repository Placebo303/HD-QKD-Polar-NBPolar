Execute the authorized Tier-X probe exactly as frozen in `TASK_PACKET.md`.
Freeze the complete embedded body before any probe call; create only the two
allowed files; preserve the accepted per-row q² `logaddexp.reduce` arithmetic;
and checkpoint the single results record after every completed cell. Restore
every temporary monkeypatch in `finally`.

Do not access artifacts/evidence roots/raw/real/EVAL data or modify production,
tests, OpenSpec, ledger, memory, or index. Obtain independent reviewer-go
focused review in the return message. Report description only. No commit/push.
