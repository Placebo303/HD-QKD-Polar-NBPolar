# PREREG_DRAFT.md — NBPOLAR-X15-STRUCTURED-CHANNEL-LIST-SURVIVAL (3-line Tier-X preregistration draft)

The frozen `workspace/probes/nbpolar_x15_structured_channel_list_survival/prereg.md` shall contain exactly these three lines; written BEFORE any generation; operator re-verifies seed freshness by repo grep at freeze.

1. Question: with a structured diagonal synthetic channel (0.75 diagonal pin from recorded raw SER ~0.25; 8-neighbor concentrated noise sharing 0.225; 96/1024 planted floor-mass cells pin kept), does the true (b,u1,u2) cell rank within top-L (L=4,8) per-position arm-table likelihood at frozen-hazard spike positions (excess ≥ 2.0 bits over prefix mean, strata [2,4)/[4,8)/[8,∞)), with symmetric L1-column ranking (Q1b) and local-spike (R=8, F-median8: h−local-median) vs mean-hazard top-52 coverage comparison on greedy-SC mismatches (Q2) — descriptive survival/coverage fractions only, no thresholds, no verdicts, SCL stays locked; structure sanity gates (pooled mismatch ∈ [0.15,0.90], Q1b non-spike L8 ≥ 0.50, spike fraction ∈ [0.05,0.60]) checked before the main question, violations stop with X15_STOP_SANITY_*.
2. Parameters: N=256, q=32, alpha=2, GF(32) poly 37, chunk_rows=512, floor 1e-15, p_b uniform over 1024 labels, per-column deterministic masses {diag 0.75; neighbors (b+1..b+8 mod 1024) share 0.225; rare15 (b+37k mod 1024, k=1..96) pre-floor 0; remaining 919 share 0.025} + 1e-15 floor + column renormalize → derive_p1/derive_p2 → probs_to_symbol_metric, K2_synth=52, table-identity master 2026092369 (reserved label, no RNG draw), block seeds 2026092370..2026092377 × 4 blocks (32 blocks, 8192 L2 positions), read-only reuse of empirical_channel/prior/algebra/transform/sc (greedy L=1 only), zero protected opens, zero tag calls.
3. Command: `cd /mnt/d/Code/HD-QKD_Polar_Comparison-nbpolar && export OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 MALLOC_ARENA_MAX=2 && timeout 300 /mnt/d/Code/HD-QKD_Polar_Comparison/.venv/bin/python workspace/probes/nbpolar_x15_structured_channel_list_survival/body.py` (interpreter fallback recorded if numpy missing; bare python/python3 never used).
