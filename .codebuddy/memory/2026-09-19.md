# 2026-09-19

## 项目现状盘点 + 与隔壁 V72P3G8「先验被 λ 抹平」问题的对照

结论要点（供后续会话直接引用）：

- 生产主链（P7 起 / P13–P19 / P20B / P20C）**不用 λ**：
  `load_v25_channel_counts` + `SUPPORT_RULE`（counts/列和 + 1e-15 floor，无 λ、无回退），
  见 `comparison_bench/src/comparison_bench/formal_ir/nbpolar/operational_f13.py:219-225`。
  1M HOLD 实测 out-of-sample NLL 0.846 bits/symbol（H1≈0.024、L2≈0.82），CE 比 1.2367。
  ⇒ 主链没有隔壁那种「λ 把先验抹平成 Bob 盲」的病。
- **但 λ 版活着且在最新一包上**：`nbpolar/per_session_calibration.py`（P20H）按
  P0/P2 Model-F concentration 程序 λ=137.3823795883264 套在 1.5M TRAIN（424,960）上，
  得 H1/H2/TOTAL = 2.006647056368773 / 1.9017235959286112 / 3.908370652297384；
  `empirical_diagnostic.py` Stage B 也仍在用 LAMBDA_STAR。违反 X07 处置
  （「用 V25 counts + 1e-15 floor-only；不改 λ、不用 Model-F 产物做目标信道」）。
- P20H 失败可基本归因于 λ：1.5M raw SER 0.2563 ≈ 1M 0.2406；oracle-L1 臂 L2 NLL
  26,809 bits = 0.818 bits/symbol，与 1M HOLD 的 0.818–0.824 相同（session 本身并不差）；
  但 λ 使 H1 从 ~0.02 抬到 2.0066 → L1 需 ≈65,750 bits 而只给 K1=319（1,595 bits），
  first_error_coord 16/L1，B0/B1 0/3，B2（不解码 L1）3/3。全块实际 f=34119/(32768×3.9084)=0.266。
  P20I 计划 K1→447（2,235 bits）仍差约 30×，按现状注定失败。
- 零成本 S0：P20H 已产出的 `calibrated_prior.npz` 含 `counts_ab`，可纯数组算
  raw-MLE+floor 的 H1/H2 与 λ 版对比（不需要新的受保护 counts 读取）。

## 其他发现的问题（方法论层面，重要）

- in-sample / out-of-sample 混淆：P16/P17 是从先验自采样；P20C/E/F 的「DEV」帧
  0..383 / 384..767 / 768..1151 落在 1M TRAIN 池 0..1199 内（P20F floor_hits_1e15=0、
  L2 NLL 0.7757 ≈ in-sample H2 0.77676、3/3 exact）。唯一真正的样本外真实运行
  P18/P19（HOLD 1600..1983）0/3，且 P19 的 true-L1 控制也 0/3。
- 工作点无余量：out-of-sample NLL 仅比 in-sample 高 6%，f=1.3 即从全对掉到全错。
- 重尾：P20H DEV 9.35%（3063/32768）查表落在 TRAIN 零计数格，贡献 21,624.8 bits 对数损失。
- 门禁缺口：P20H 三重门禁只查 DEV vs VAL/HOLD，不查 DEV vs 建表 TRAIN；冻结文本又假设
  TRAIN 基 0、DEV 0..383 ⊂ TRAIN 0..1659，与 9.35% 零计数不一致 → 重叠关系未知且未被门禁。
- 预算与 H 脱钩：绝对 bit 预算（34119）跨 session 冻结，未按 session 自身 H 重算。
- 文档滞后：CURRENT_TASK.md 仍写 P20A 待授权，P20A STATUS.yaml 已是 IMPLEMENTATION_ACCEPTED。

## 复审（同日晚）：λ 处置已闭环，L2 成为瓶颈；P20O 待授权；未提交改动堆积

- λ 处置闭环：X08 探针定量确认（λ prior H_total 3.9084 vs raw+floor 0.8256；L1 side info 65754 vs 826 bits；K_total 33285 vs 7020），decision-log 已定「raw+floor 为 canonical、λ 仅对照臂」，P20L 被 P20M 零消耗取代。P20H 那条线的 λ 问题不再是悬案。
- 后继进展（上次对话未覆盖）：P20M（raw prior + session H 预算）首次真样本外 → G1 L1 突破 2/3、首错转 L2；X09-R1 裁决 H1 预算不足 NOT SUPPORTED、H3 floor 重尾 NOT SUPPORTED、H2（order/metric 不泛化）NOT DECIDABLE 需逐位置仪表；P20N（alt-L2 Laplace α=1，1.5M HOLD）B 1/4 首次操作性恢复（b_restored=1），14/16 失败全 L2，已接受 descriptive。
- 当前硬闸门：P20O（2M 首次启用，maintain confirmation）Stage A 完成 + Pre-EXECUTE PASS，等待 Stage B 授权文本；冻结 K1 334/K2 6746/7080、H 0.8326、D2 margin 4791.1 bits。
- 新风险（工程）：工作树 89 条未提交（27 M + 62 ??，含 P20A–P20O、X08/X09、probes、decision-log、源码/测试、19 个 queue 目录）；最后 commit 2026-09-16 21:14。所有已接受证据链均未入 git，存在丢失风险；`.codebuddy/` 未跟踪。建议在 P20O 前后收口提交（不由 agent 擅自提交）。
- 新风险（科学）：P20O 仪表仍是 9 个标量级，未必足以裁决 H2；alt-L2 恢复机制不明（CE/hazard 更高却恢复 1 块，疑与 order 有关）；f=1.3 工作点无余量；统计力度仅 1/4 与 5 块/20 records（仅 descriptive）。
