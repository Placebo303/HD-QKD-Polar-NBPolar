# Proposal: hard-L1 conditioning penalty discriminator

X02 identified a dependent-L2 synthetic point where the oracle arm is saturated
but the hard-candidate operational arm is substantially worse. Run one powered
Tier-Y paired gate to decide whether this mechanism is material. A confirmed
penalty stops direct scaling of the current hard-conditioning architecture and
routes later exploration toward list/soft provenance-safe coupling.
