# Proposal: NB-Polar probe tier and slimmer decision gates

Introduce an explicit non-claim exploratory tier so cheap synthetic sensitivity
work can precede expensive lifecycle freezes. Preserve the existing decision
tier for claim-bearing results. Add a delta-successor path that reuses unchanged
predecessor contracts.
