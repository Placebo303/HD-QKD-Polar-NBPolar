1. Question: Can the two frozen X13 probe-local memory-lifetime alternatives (lifetime_only release ordering; owned_log owned in-place log conversion with direct sc_decode) complete the full large-N descriptive evidence that X13 could not finish (its combined 400 s child budget was shorter than the observed 425.5 s workload for 4xN=65536 plus 2xN=262144 blocks), reproducing X13's deterministic N=64 finite and X11-C3 controls exactly and recording per-block status, exact semantic scalar outcomes, wall, peak HWM and stage/live-array ledgers for 6/6 blocks per alternative in four fresh isolated 2 GiB children (Tier-X descriptive only; new probe not a rerun/repair of X13; X13 stays immutable and its 5 partial large-N blocks count for nothing; no attempt, threshold, winner, candidate, acceptance, or status change; do not rerun P12).
2. Parameters: inheritance=X13 files workspace/probes/nbpolar_x13_metric_memory_lifetime/prereg.md (sha256 993b38437c999f9ef60f8f759bfef2c7f2adefcfe5e019774f5f424053b6e717) + results.json (sha256 6c0a39b375fba41cde4c6bc4f0290be26a9aaa7719f76687a6868478ffa314ab, status STOPPED_RESOURCE_TIMEOUT) read-only to verify identities and inherit the independently reviewed 10/10 small-N parity (all PARITY_KEYS plus outcome True at N=64/N=256 x finite/exactzero/known/impossible/x11c3) and the 5 recorded partial large-N stages (lifetime_only N=65536 blocks 0..3 outcomes mismatch_untagged walls 31.76/32.26/31.94/32.48 plus N=262144 block0 mismatch_untagged wall 148.54, descriptive reference only, excluded from completeness); seed=2026091840 single master stream; GF32 polynomial 37 alpha=2 chunk_rows=512 (accepted _minus_block default asserted read-only; accepted prior builders/converter/validation, accepted chunked sc_decode, accepted polar_transform imported read-only, never modified); tables built in-body in order P1=rng.uniform(0,1,(32,1024)) column-normalized, P2=rng.uniform(0,1,(32,1024,32)) last-axis-normalized, p_b=rng.uniform(0,1,(1024,)) normalized with expected sums [1024.0, 32768.0, 1.0] and shapes [32,1024]/[32,1024,32]/[1024] (P1 stored [U1,B]=(32,1024) per accepted builder shape; Bob-row view [1024,32]); Bob sampled uniform integers 0..1023 (p_b recorded family table); parent replays the exact X13 N=64 five-case prefix in order (finite, exactzero, known, impossible with t=0..199 ImpossibleDisclosedValueError search, x11c3 with undisclosed-baseline argmax C3 disclosures D1=[3,10,17,24,31,38,45,52] D2=[5,7,9,16,18,20,27,29,31,38,40,42,49,51,60,62]) on one shared rng and gates ONLY on N64_finite (expected outcome mismatch_untagged, disclosures null/null, extra {k1:8,k2:16}, baseline anchors l1_logp 8e542d2ddfe0ce81937e2c2a4722163072d114ab071f85570751a360a4fa4ef1 l2_logp 5da60f9616cd327f7ae54f7b86ef3d8b58f91d59bac1419a069d00496475a6ba sc1_x fc0bf1efa89f2e3c9d30204fcbb02c8e973fdef680bb46ca9b2192728ea5657f sc2_x 336f82a7318b90955247079bc61fcb9f136f9dd0e8914f4428e8eff338ea3fe6) and N64_x11c3 (expected outcome mismatch_untagged, disclosures above, extra {k1:8,k2:16,c3_values_from_undisclosed_argmax:True}, baseline anchors l1_logp b0b7bb7f411561cab01311ceb165adbe295f7183c2d2c8226868af7629640fb6 l2_logp c78960c95a5e45d61a279adcb9e63fe23152b6f86c6c6a18ec3e0e7dcb852e6c sc1_x aef0559fde0d352d2731441321603efc96eb3269d2ffbd71c9c2e185f856fe17 sc2_x 6ad0bd83928c0a5dba53abc9bb8e9d844ab95ea6d885e7650dcd3f35080ace82) requiring parity_all True plus every parity key True plus all three arm outcome strings equal plus baseline anchors equal (wall excluded); any gate mismatch stops immediately as semantic/inheritance failure with no child launch and no rerun; small-N exactness rule per case one shared base L1 prob block, per-arm copy plus case mask, identical inputs to baseline/lifetime_only/owned_log with masks finite none, exactzero L1 rows j%4==0 odd symbols 0 plus L2 rows j%4==1 even symbols 0, known truth-U disclosures, impossible L1 even rows symbol-0-only with found D1 disclosures plus D2 truth disclosures, x11c3 L1 rows j%3==0 odd symbols 0; D1=sorted((arange(k1)*7+3)%N) D2=sorted((arange(k2)*11+5)%N) with k1=8 k2=16 at N=64; alternatives frozen exactly as X13: lifetime_only preserves accepted builders/converter then dels L1 probs/metric/SCResult plus L2 probs/SCResult at last use and reduces outcomes to scalars, owned_log adds owned float64 L2 copy (owndata/base/shares_memory audited) validated under accepted contract then np.log plus row normalization in place with direct sc_decode and frozen enum provenance scalar, never mutating caller-owned/view arrays; four fresh child cells in frozen order with ulimit -v 2097152 (RLIMIT_AS 2 GiB via preexec plus in-body) and independent timeouts: cell1 lifetime_only N=65536 4 sequential blocks 240 s, cell2 lifetime_only N=262144 2 sequential blocks 600 s, cell3 owned_log N=65536 4 sequential blocks 240 s, cell4 owned_log N=262144 2 sequential blocks 600 s; RNG-position rule: every child roots rng at master seed 2026091840 and rebuilds identical P1/P2/p_b, N=65536 cells draw their 4 (high,low,bob) triples directly, each N=262144 cell first draws-and-discards the same four N=65536 triples (per-block high/low/bob sha recorded as preconsume) before drawing its 2 large triples, reproducing X13's single-stream positions with identical cross-alternative inputs as in X13; stream-position check: parent requires large-cell preconsume sha triples exactly equal the same-alternative small-cell block input sha triples (mismatch stops immediately as semantic/inheritance failure); each child reports every block's status block_complete, exact semantic scalar outcome, per-block peak, input shas, wall plus tables-immutable flag, ledger snaps (post_L1_decode/post_L1_release/post_L2_decode/post_L2_release/variant_return/group_done with live N x 32 float64 counts and formula bytes) and final record JSON on stdout with X13R1STAGE cell/N/block/outcome/wall lines on stderr; parent drains stdout+stderr concurrently, captures exit/wall/timeout/tails/MemoryError scan, checkpoints the single results.json at init, tables, each N=64 case, every live block message, each attempt end, each child termination, each stream check and finalize; child timeout or MemoryError is descriptive resource failure finishing that cell's record then STOP without launching later cells and never rerun; execution-code error (nonzero/empty-record exit without timeout or memory sign) may be rerun ONCE with unchanged freeze and both attempts fully recorded, resource/semantic failures never rerun; completeness is 6/6 blocks per alternative (12 total) with all expected stage records, X13 partials excluded; results.json single record holds env/params/shas, inheritance record (X13 shas plus matched parity values), parent controls, per-cell attempts/exit/stage/wall/RSS/MemoryError/stream checks/ledgers, 6/6 counts, execution/error accounting, operator focused review and a descriptive 4-way support statement naming exactly lifetime_only, owned_log, BOTH or NEITHER for a future P12-R1 with no winner/threshold/claim; write scope is only this prereg.md plus results.json in workspace/probes/nbpolar_x13r1_metric_memory_completion (/tmp scratch /tmp/x13r1_body.py plus parent stdio logs allowed); forbidden: V25 NPZ, evidence roots, official seeds, raw/held-out/real/EVAL data, tags (tag path skipped at all N; accepted Toeplitz index ~1.3 GiB at N=262144 would confound), FWHT/APP/SCL imports, scientific gates, production/test/OpenSpec/ledger/memory/index edits, commit/push.
3. Command: cwd=/mnt/d/Code/HD-QKD_Polar_Comparison-nbpolar; PYTHONDONTWRITEBYTECODE=1 ulimit -v 2097152; timeout 1900 /mnt/d/Code/HD-QKD_Polar_Comparison/.venv/bin/python /tmp/x13r1_body.py (staged byte-identically from the prereg python fence; body self-verifies staged bytes equal embedded bytes before any probe call; single execution, attempt 1).

```python
"""X13R1 Tier-X frozen body: metric-memory completion probe, descriptive only.

New probe (not a rerun/repair of X13): keeps X13 immutable, inherits its
independently reviewed 10/10 small-N parity, verifies the deterministic N=64
finite and X11-C3 controls in the parent, then runs four fresh isolated 2 GiB
child cells (each alternative at N=65536x4 and N=262144x2) with per-block
checkpointing. Same deterministic injected P1/P2/p_b generation, master probe
seed, GF(32) poly 37 alpha 2, chunk_rows 512, and exact lifetime_only /
owned_log alternatives as X13. No semantic or implementation change from X13
in the pipeline; only the cell split, stream-position recording, and live
per-message checkpointing differ. Injected tables and synthetic vectors only.
No artifact/evidence/official-seed/raw/held-out/real/EVAL/tag/FWHT/APP/SCL/
gate access. No threshold, winner, candidate, acceptance, or label. Writes
only results.json in its probe dir.
"""

from __future__ import annotations

import hashlib
import inspect
import json
import os
import platform
import re
import resource
import subprocess
import sys
import threading
import time
import traceback
from pathlib import Path

sys.dont_write_bytecode = True

T0 = time.monotonic()

REPO = Path("/mnt/d/Code/HD-QKD_Polar_Comparison-nbpolar")
PROBE_DIR = REPO / "workspace" / "probes" / "nbpolar_x13r1_metric_memory_completion"
PREREG_PATH = PROBE_DIR / "prereg.md"
RESULTS_PATH = PROBE_DIR / "results.json"
BODY_PATH = Path("/tmp/x13r1_body.py")
INTERPRETER = "/mnt/d/Code/HD-QKD_Polar_Comparison/.venv/bin/python"
COMMAND_TEXT = ("PYTHONDONTWRITEBYTECODE=1 ulimit -v 2097152; timeout 1900 "
                + INTERPRETER + " /tmp/x13r1_body.py")

SEED = 2026091840
Q = 32
ALPHA = 2
POLY = 37
NB = 1024
VSZ = 2 * 1024 ** 3
VSZ_KB = 2097152
TOTAL_WALL_S = 1900
CELLS = (
    {"cell": 1, "alternative": "lifetime_only", "N": 65536, "blocks": 4,
     "timeout_s": 240},
    {"cell": 2, "alternative": "lifetime_only", "N": 262144, "blocks": 2,
     "timeout_s": 600},
    {"cell": 3, "alternative": "owned_log", "N": 65536, "blocks": 4,
     "timeout_s": 240},
    {"cell": 4, "alternative": "owned_log", "N": 262144, "blocks": 2,
     "timeout_s": 600},
)
PRECONSUME_N = 65536
PRECONSUME_BLOCKS = 4
ATTEMPT = int(os.environ.get("X13R1_ATTEMPT", "1"))
IS_CHILD = len(sys.argv) > 1 and sys.argv[1] == "--child"
CHILD_ALT = sys.argv[2] if IS_CHILD and len(sys.argv) > 2 else None
CHILD_N = int(sys.argv[3]) if IS_CHILD and len(sys.argv) > 3 else None
CHILD_BLOCKS = int(sys.argv[4]) if IS_CHILD and len(sys.argv) > 4 else None
HWM_CAVEAT = ("ledger counts named live N x 32 pipeline arrays only, not SC "
              "temporaries, interpreter overhead, or non-N x 32 arrays; "
              "ru_maxrss/VmHWM is the high-water mark")
OUTCOME_KEYS = ("outcome", "label_match", "l1_provenance", "l2_provenance",
                "l1_ok", "l2_ok", "l1_error", "l2_error",
                "key_dependent_bits", "public_control_bits", "tag_invoked")
PARITY_KEYS = ("l1_logp_sha", "l1_support_sha", "l1_provenance", "l1_exc",
               "sc1_u_sha", "sc1_x_sha", "sc1_dm_sha", "sc1_ds_sha",
               "sc1_status", "l2_logp_sha", "l2_support_sha", "l2_provenance",
               "l2_exc", "sc2_u_sha", "sc2_x_sha", "sc2_dm_sha", "sc2_ds_sha",
               "sc2_status")
PARENT_CASES = ("finite", "exactzero", "known", "impossible", "x11c3")
ALTS = ("baseline", "lifetime_only", "owned_log")
ANCHOR_KEYS = ("l1_logp_sha", "l2_logp_sha", "sc1_x_sha", "sc2_x_sha")
EXPECTED = {
    "N64_finite": {
        "outcome": "mismatch_untagged",
        "disclosures": {"d1": None, "d2": None},
        "extra": {"k1": 8, "k2": 16},
        "anchor": {
            "l1_logp_sha": "8e542d2ddfe0ce81937e2c2a4722163072d114ab071f85570751a360a4fa4ef1",
            "l2_logp_sha": "5da60f9616cd327f7ae54f7b86ef3d8b58f91d59bac1419a069d00496475a6ba",
            "sc1_x_sha": "fc0bf1efa89f2e3c9d30204fcbb02c8e973fdef680bb46ca9b2192728ea5657f",
            "sc2_x_sha": "336f82a7318b90955247079bc61fcb9f136f9dd0e8914f4428e8eff338ea3fe6"}},
    "N64_x11c3": {
        "outcome": "mismatch_untagged",
        "disclosures": {
            "d1": [3, 10, 17, 24, 31, 38, 45, 52],
            "d2": [5, 7, 9, 16, 18, 20, 27, 29, 31, 38, 40, 42, 49, 51,
                   60, 62]},
        "extra": {"k1": 8, "k2": 16,
                  "c3_values_from_undisclosed_argmax": True},
        "anchor": {
            "l1_logp_sha": "b0b7bb7f411561cab01311ceb165adbe295f7183c2d2c8226868af7629640fb6",
            "l2_logp_sha": "c78960c95a5e45d61a279adcb9e63fe23152b6f86c6c6a18ec3e0e7dcb852e6c",
            "sc1_x_sha": "aef0559fde0d352d2731441321603efc96eb3269d2ffbd71c9c2e185f856fe17",
            "sc2_x_sha": "6ad0bd83928c0a5dba53abc9bb8e9d844ab95ea6d885e7650dcd3f35080ace82"}},
}
EXPECTED_TABLE_SUMS = [1024.0, 32768.0, 1.0]
X13_INHERIT = {
    "x13_prereg_sha256":
        "993b38437c999f9ef60f8f759bfef2c7f2adefcfe5e019774f5f424053b6e717",
    "x13_results_sha256":
        "6c0a39b375fba41cde4c6bc4f0290be26a9aaa7719f76687a6868478ffa314ab",
    "x13_status": "STOPPED_RESOURCE_TIMEOUT",
    "x13_parity": "10/10 small-N cases parity_all True",
    "x13_partials_excluded": True,
    "x13_partial_stages": [
        {"N": 65536, "block": 0, "outcome": "mismatch_untagged",
         "wall_s": 31.76},
        {"N": 65536, "block": 1, "outcome": "mismatch_untagged",
         "wall_s": 32.26},
        {"N": 65536, "block": 2, "outcome": "mismatch_untagged",
         "wall_s": 31.94},
        {"N": 65536, "block": 3, "outcome": "mismatch_untagged",
         "wall_s": 32.48},
        {"N": 262144, "block": 0, "outcome": "mismatch_untagged",
         "wall_s": 148.54}],
}

try:
    resource.setrlimit(resource.RLIMIT_AS, (VSZ, VSZ))
    RLIMIT_OK = True
except Exception:
    RLIMIT_OK = False

import numpy as np


def _sha(b):
    return hashlib.sha256(b).hexdigest()


def sha_arr(a):
    return _sha(np.ascontiguousarray(a).tobytes())


def rss_peak():
    ru = int(resource.getrusage(resource.RUSAGE_SELF).ru_maxrss) * 1024
    hwm = None
    try:
        for line in open("/proc/self/status", encoding="utf-8"):
            if line.startswith("VmHWM:"):
                hwm = int(line.split()[1]) * 1024
                break
    except OSError:
        hwm = None
    vals = [v for v in (ru, hwm) if v]
    return {"ru_maxrss_bytes": ru, "vmhwm_bytes": hwm,
            "peak_bytes": max(vals) if vals else None}


PROBE_DIR.mkdir(parents=True, exist_ok=True)
_prereg_bytes = PREREG_PATH.read_bytes()
PREREG_SHA = _sha(_prereg_bytes)
_own_bytes = Path(__file__).read_bytes()
BODY_SHA = _sha(_own_bytes)
_pt = _prereg_bytes.decode("utf-8")
_FENCE = "```python\n"
_f0 = _pt.index(_FENCE) + len(_FENCE)
_f1 = _pt.index("\n```", _f0)
BODY_MATCH = bool(_pt[_f0:_f1].encode("utf-8") == _own_bytes)

FIELD = None
G = {"completed_stages": []}
_LOCK = threading.Lock()


def checkpoint(status, extra=None):
    with _LOCK:
        G["status"] = status
        G["wall_s"] = time.monotonic() - T0
        G["rss_peak"] = rss_peak()
        if extra:
            G.update(extra)
        RESULTS_PATH.write_text(json.dumps(G, indent=1) + "\n",
                                encoding="utf-8")


def note_checkpoint(label):
    with _LOCK:
        G.setdefault("checkpoints", []).append(
            {"label": label, "wall_s": time.monotonic() - T0})
        G["wall_s"] = time.monotonic() - T0
        RESULTS_PATH.write_text(json.dumps(G, indent=1) + "\n",
                                encoding="utf-8")


def stop_parent(reason, detail):
    checkpoint("STOPPED_" + str(reason).upper(), {"stop_detail": detail})
    print("STOP: %s %s" % (reason, json.dumps(detail)[:500]))
    raise SystemExit(2)


def import_accepted(allow_stop=True):
    def _fail(reason, detail):
        if allow_stop and not IS_CHILD:
            stop_parent(reason, detail)
        raise RuntimeError("accepted-identity failure: %s %s"
                           % (reason, detail))
    global FIELD
    sys.path.insert(0, str(REPO / "comparison_bench" / "src"))
    import comparison_bench.formal_ir.nbpolar.sc as sc_mod
    from comparison_bench.formal_ir.nbpolar.algebra import make_gf32
    from comparison_bench.formal_ir.nbpolar.prior import (
        Provenance, build_p1_metrics, gather_p2_metrics, probs_to_symbol_metric)
    from comparison_bench.formal_ir.nbpolar.sc import (
        ImpossibleDisclosedValueError, sc_decode)
    from comparison_bench.formal_ir.nbpolar.transform import polar_transform
    chunk_default = inspect.signature(sc_mod._minus_block).parameters[
        "chunk_rows"].default
    field = make_gf32()
    if int(field.q) != Q:
        _fail("field_identity", {"q": int(field.q)})
    if int(getattr(field, "primitive_polynomial", -1)) != POLY:
        _fail("field_identity",
              {"poly": getattr(field, "primitive_polynomial", None)})
    if int(chunk_default) != 512:
        _fail("chunk_identity", {"chunk_rows_default": chunk_default})
    FIELD = field
    return {"sc_mod": sc_mod,
            "Provenance": Provenance,
            "build_p1_metrics": build_p1_metrics,
            "gather_p2_metrics": gather_p2_metrics,
            "probs_to_symbol_metric": probs_to_symbol_metric,
            "ImpossibleDisclosedValueError": ImpossibleDisclosedValueError,
            "sc_decode": sc_decode,
            "polar_transform": polar_transform,
            "chunk_rows_default": int(chunk_default)}


def build_tables(rng, acc):
    p1_raw = rng.uniform(0.0, 1.0, (Q, NB))
    p1 = p1_raw / p1_raw.sum(axis=0, keepdims=True)
    p2_raw = rng.uniform(0.0, 1.0, (Q, NB, Q))
    p2 = p2_raw / p2_raw.sum(axis=2, keepdims=True)
    pb_raw = rng.uniform(0.0, 1.0, (NB,))
    pb = pb_raw / pb_raw.sum()
    return p1, p2, pb


class Ledger:
    def __init__(self):
        self.reg = {}
        self.snaps = []

    def reg_arr(self, name, arr):
        a = np.asarray(arr)
        self.reg[name] = {"shape": [int(v) for v in a.shape],
                          "dtype": str(a.dtype), "nbytes": int(a.nbytes),
                          "referenced": True}

    def rel(self, name):
        if name in self.reg:
            self.reg[name]["referenced"] = False

    def reset(self):
        self.reg = {}

    def snap(self, stage, n):
        live = {k: dict(v) for k, v in self.reg.items()
                if v["referenced"]}
        n32 = {k: v for k, v in live.items()
               if len(v["shape"]) == 2 and v["shape"][0] == n
               and v["shape"][1] == 32 and v["dtype"] == "float64"}
        self.snaps.append({
            "stage": stage, "N": n, "live": live,
            "live_total_bytes": sum(v["nbytes"] for v in live.values()),
            "live_n32_float64_count": len(n32),
            "live_n32_actual_bytes": sum(v["nbytes"] for v in n32.values()),
            "live_n32_formula_bytes": len(n32) * n * 32 * 8,
            "released": sorted(k for k, v in self.reg.items()
                               if not v["referenced"])})


def owned_log_convert(probs):
    arr = np.asarray(probs)
    if arr.dtype.kind == "b":
        raise TypeError(
            "metric contract: probs must hold numbers, got boolean input")
    if arr.ndim != 2 or arr.shape[1] != Q:
        raise ValueError(
            "metric contract: probs must have shape (N,32), got shape %s"
            % (arr.shape,))
    try:
        owned = np.array(arr, dtype=np.float64, copy=True)
    except (TypeError, ValueError) as exc:
        raise ValueError(
            "metric contract: probs must be numeric (%s)" % exc)
    if not np.isfinite(owned).all():
        raise ValueError("metric contract: probs must be finite")
    if (owned < 0).any():
        raise ValueError("metric contract: probs must be non-negative")
    if (owned.sum(axis=1) <= 0).any():
        raise ValueError(
            "metric contract: every probs row needs positive mass")
    audit = {"base_is_none": owned.base is None,
             "owndata": bool(owned.flags.owndata),
             "shares_memory_with_input": bool(np.shares_memory(owned, arr))}
    with np.errstate(divide="ignore"):
        owned[owned == 0.0] = -np.inf
        np.log(owned, out=owned, where=owned > 0)
    with np.errstate(invalid="ignore"):
        lse = np.logaddexp.reduce(owned, axis=1)
    owned -= lse[:, None]
    return owned, audit


def run_variant(p1, bob, high_true, low_true, d1, v1, d2, v2, l2_mask,
                variant, led, n, btag, acc):
    Provenance = acc["Provenance"]
    probs_to_symbol_metric = acc["probs_to_symbol_metric"]
    gather_p2_metrics = acc["gather_p2_metrics"]
    sc_decode = acc["sc_decode"]
    det = {"variant": variant}
    p1_sha0 = sha_arr(p1)
    led.reg_arr(btag + "p1_probs", p1)
    m1 = probs_to_symbol_metric(p1, provenance=Provenance.PRIOR_ONLY)
    led.reg_arr(btag + "m1_logp", m1.logp)
    det["l1_logp_sha"] = sha_arr(m1.logp)
    det["l1_support_sha"] = sha_arr(np.isfinite(m1.logp))
    det["l1_provenance"] = m1.provenance.value
    det["l1_provenance_is_enum"] = isinstance(m1.provenance, Provenance)
    l1_exc = None
    sc1 = None
    try:
        sc1 = sc_decode(m1.logp, field=FIELD, alpha=ALPHA,
                        known_positions=d1, known_values=v1)
    except Exception as exc:
        l1_exc = [type(exc).__name__, str(exc)]
    det["l1_exc"] = l1_exc
    high_hat = None
    if sc1 is not None:
        led.reg_arr(btag + "sc1_dm", sc1.decision_metrics)
        det["sc1_u_sha"] = sha_arr(sc1.u_hat)
        det["sc1_x_sha"] = sha_arr(sc1.x_hat)
        det["sc1_dm_sha"] = sha_arr(sc1.decision_metrics)
        det["sc1_ds_sha"] = sha_arr(sc1.decision_log_scores)
        det["sc1_status"] = sc1.status
        det["sc1_known"] = int(sc1.known_count)
        high_hat = np.array(sc1.x_hat, copy=True)
    led.snap(btag + "post_L1_decode", n)
    keep = {}
    if variant == "baseline":
        keep = {"p1": p1, "m1": m1, "sc1": sc1}
    else:
        det["p1_immutable"] = bool(sha_arr(p1) == p1_sha0)
        led.rel(btag + "p1_probs")
        led.rel(btag + "m1_logp")
        if sc1 is not None:
            led.rel(btag + "sc1_dm")
        del m1
        if sc1 is not None:
            del sc1
        del p1
    led.snap(btag + "post_L1_release", n)
    l2_exc = None
    sc2 = None
    low_hat = None
    label_hat = None
    p2g = None
    m2 = None
    owned = None
    if high_hat is not None:
        p2g = gather_p2_metrics(bob[None, :], high_hat[None, :], P2TAB)[0]
        if l2_mask is not None:
            p2g = l2_mask(p2g)
        led.reg_arr(btag + "p2_probs", p2g)
        if variant == "owned_log":
            owned, audit = owned_log_convert(p2g)
            det["owned_audit"] = audit
            det["l2_provenance"] = Provenance.CANDIDATE_CONDITIONED.value
            det["l2_provenance_is_enum"] = True
            led.reg_arr(btag + "l2_logp_owned", owned)
            det["l2_logp_sha"] = sha_arr(owned)
            det["l2_support_sha"] = sha_arr(np.isfinite(owned))
            in_sha = det["l2_logp_sha"]
            try:
                sc2 = sc_decode(owned, field=FIELD, alpha=ALPHA,
                                known_positions=d2, known_values=v2)
            except Exception as exc:
                l2_exc = [type(exc).__name__, str(exc)]
            det["l2_input_immutable"] = bool(sha_arr(owned) == in_sha)
        else:
            m2 = probs_to_symbol_metric(
                p2g, provenance=Provenance.CANDIDATE_CONDITIONED)
            led.reg_arr(btag + "m2_logp", m2.logp)
            det["l2_logp_sha"] = sha_arr(m2.logp)
            det["l2_support_sha"] = sha_arr(np.isfinite(m2.logp))
            det["l2_provenance"] = m2.provenance.value
            det["l2_provenance_is_enum"] = isinstance(m2.provenance,
                                                     Provenance)
            try:
                sc2 = sc_decode(m2.logp, field=FIELD, alpha=ALPHA,
                                known_positions=d2, known_values=v2)
            except Exception as exc:
                l2_exc = [type(exc).__name__, str(exc)]
        det["l2_exc"] = l2_exc
        if sc2 is not None:
            led.reg_arr(btag + "sc2_dm", sc2.decision_metrics)
            det["sc2_u_sha"] = sha_arr(sc2.u_hat)
            det["sc2_x_sha"] = sha_arr(sc2.x_hat)
            det["sc2_dm_sha"] = sha_arr(sc2.decision_metrics)
            det["sc2_ds_sha"] = sha_arr(sc2.decision_log_scores)
            det["sc2_status"] = sc2.status
            det["sc2_known"] = int(sc2.known_count)
            low_hat = np.array(sc2.x_hat, copy=True)
            label_hat = (low_hat + 32 * high_hat).astype(np.int64)
    else:
        det["l2_exc"] = None
    led.snap(btag + "post_L2_decode", n)
    if variant == "baseline":
        keep.update({"p2g": p2g if high_hat is not None else None,
                     "m2": m2 if (high_hat is not None
                                  and variant != "owned_log") else None,
                     "sc2": sc2})
    else:
        led.rel(btag + "p2_probs")
        if high_hat is not None:
            if variant == "owned_log":
                led.rel(btag + "l2_logp_owned")
            else:
                led.rel(btag + "m2_logp")
        if sc2 is not None:
            led.rel(btag + "sc2_dm")
        if high_hat is not None:
            del p2g
            if variant == "owned_log":
                del owned
            else:
                del m2
        if sc2 is not None:
            del sc2
    led.snap(btag + "post_L2_release", n)
    labels_true = (low_true + 32 * high_true).astype(np.int64)
    label_match = (bool(np.array_equal(label_hat, labels_true))
                   if label_hat is not None else False)
    if l1_exc is not None:
        outcome = "decode_failed"
    elif l2_exc is not None:
        outcome = "decode_failed"
    elif label_match:
        outcome = "exact_untagged"
    else:
        outcome = "mismatch_untagged"
    k1n = 0 if d1 is None else int(len(np.asarray(d1)))
    k2n = 0 if d2 is None else int(len(np.asarray(d2)))
    result = {"outcome": outcome, "label_match": label_match,
              "l1_provenance": det.get("l1_provenance"),
              "l2_provenance": det.get("l2_provenance"),
              "l1_ok": l1_exc is None, "l2_ok": l2_exc is None,
              "l1_error": l1_exc[0] if l1_exc else None,
              "l2_error": l2_exc[0] if l2_exc else None,
              "key_dependent_bits": 5 * k1n + 5 * k2n,
              "public_control_bits": 0, "tag_invoked": False,
              "N": n, "variant": variant}
    del high_hat, low_hat, label_hat, labels_true
    if variant == "baseline":
        det["p1_immutable"] = bool(sha_arr(p1) == p1_sha0)
        for name in list(led.reg):
            if name.startswith(btag):
                led.rel(name)
        del keep
        del p1, m1, sc1, p2g, m2, sc2
    led.snap(btag + "variant_return", n)
    return result, det


def mask_l1_A(a):
    a[0::4, 1::2] = 0.0
    return a


def mask_l2_B(a):
    a[1::4, 0::2] = 0.0
    return a


def mask_l1_D(a):
    a[0::2, 1:] = 0.0
    return a


def mask_l1_C(a):
    a[0::3, 1::2] = 0.0
    return a


def dset(n, k, mult, off):
    return np.sort(((np.arange(k) * mult + off) % n).astype(np.int64))


def gather_l1(bob, acc):
    return acc["build_p1_metrics"](bob[None, :], P1TAB)[0]


def parity_case(n, case, rng, acc, tables0):
    polar_transform = acc["polar_transform"]
    ImpossibleDisclosedValueError = acc["ImpossibleDisclosedValueError"]
    k1 = 8 if n == 64 else 45
    k2 = 16 if n == 64 else 110
    high = rng.integers(0, Q, n).astype(np.int64)
    low = rng.integers(0, Q, n).astype(np.int64)
    bob = rng.integers(0, NB, n).astype(np.int64)
    base = gather_l1(bob, acc)
    u1 = polar_transform(high, field=FIELD, alpha=ALPHA)
    u2 = polar_transform(low, field=FIELD, alpha=ALPHA)
    d1 = dset(n, k1, 7, 3)
    d2 = dset(n, k2, 11, 5)
    l2_mask = None
    det_extra = {"k1": k1, "k2": k2}
    if case == "finite":
        masks = [None]
        dd1, vv1, dd2, vv2 = None, None, None, None
    elif case == "exactzero":
        masks = [mask_l1_A]
        dd1, vv1, dd2, vv2 = None, None, None, None
        l2_mask = mask_l2_B
    elif case == "known":
        masks = [None]
        dd1, vv1, dd2, vv2 = d1, np.array(u1[d1], copy=True), d2, \
            np.array(u2[d2], copy=True)
    elif case == "impossible":
        masks = [mask_l1_D]
        dd2, vv2 = d2, np.array(u2[d2], copy=True)
        found = None
        for t in range(200):
            pos = ((t * 7 + np.arange(16) * 3) % n).astype(np.int64)
            vals = rng.integers(0, Q, 16).astype(np.int64)
            trial = base.copy()
            mask_l1_D(trial)
            led1 = Ledger()
            out, det = run_variant(trial, bob, high, low, pos, vals, dd2,
                                   vv2, None, "baseline", led1, n,
                                   "search_", acc)
            del trial
            got = det.get("l1_exc") or det.get("l2_exc")
            if got is not None and got[0] == "ImpossibleDisclosedValueError":
                found = {"t": t, "pos": pos.tolist(),
                         "vals": vals.tolist(),
                         "layer": ("L1" if det.get("l1_exc") else "L2"),
                         "message": got[1]}
                break
        if found is None:
            return {"N": n, "case": case, "no_impossible_case": True,
                    "search_tries": 200}
        det_extra["search"] = found
        dd1 = np.array(found["pos"], dtype=np.int64)
        vv1 = np.array(found["vals"], dtype=np.int64)
    elif case == "x11c3":
        masks = [mask_l1_C]
        trial = base.copy()
        mask_l1_C(trial)
        sel_m1 = acc["probs_to_symbol_metric"](
            trial, provenance=acc["Provenance"].PRIOR_ONLY)
        sel_sc1 = acc["sc_decode"](sel_m1.logp, field=FIELD, alpha=ALPHA,
                                   known_positions=None, known_values=None)
        sel_high = np.array(sel_sc1.x_hat, copy=True)
        sel_p2 = acc["gather_p2_metrics"](bob[None, :], sel_high[None, :],
                                          P2TAB)[0]
        sel_m2 = acc["probs_to_symbol_metric"](
            sel_p2,
            provenance=acc["Provenance"].CANDIDATE_CONDITIONED)
        sel_sc2 = acc["sc_decode"](sel_m2.logp, field=FIELD, alpha=ALPHA,
                                   known_positions=None, known_values=None)
        dm1 = np.array(sel_sc1.decision_metrics, copy=True)
        dm2 = np.array(sel_sc2.decision_metrics, copy=True)
        del sel_m1, sel_sc1, sel_high, sel_p2, sel_m2, sel_sc2, trial
        dd1 = d1
        vv1 = np.array([int(np.argmax(r)) for r in dm1[d1.tolist()]],
                       dtype=np.int64)
        dd2 = d2
        vv2 = np.array([int(np.argmax(r)) for r in dm2[d2.tolist()]],
                       dtype=np.int64)
        if not all(bool(np.isfinite(r[v])) for r, v in
                   zip(dm1[d1.tolist()], vv1.tolist())):
            return {"N": n, "case": case, "c3_support_selection": False}
        if not all(bool(np.isfinite(r[v])) for r, v in
                   zip(dm2[d2.tolist()], vv2.tolist())):
            return {"N": n, "case": case, "c3_support_selection": False}
        det_extra["c3_values_from_undisclosed_argmax"] = True
        del dm1, dm2
    else:
        raise ValueError("unknown case " + str(case))
    arms = {}
    led = Ledger()
    for arm in ALTS:
        t = time.perf_counter()
        inp = base.copy()
        for m in masks:
            if m is not None:
                inp = m(inp)
        out, det = run_variant(inp, bob, high, low, dd1, vv1, dd2, vv2,
                               l2_mask, arm, led,
                               n, "N%d_%s_%s_" % (n, case, arm), acc)
        det["wall_s"] = time.perf_counter() - t
        arms[arm] = {"outcome": out, "detail": det}
        del inp
    parity = {}
    b0 = arms["baseline"]["detail"]
    b0o = arms["baseline"]["outcome"]
    for key in PARITY_KEYS:
        vals = [arms[a]["detail"].get(key, "ABSENT") for a in ALTS]
        parity[key] = bool(vals[0] == vals[1] == vals[2])
    outs = [tuple(arms[a]["outcome"][k] for k in OUTCOME_KEYS)
            for a in ALTS]
    parity["outcome"] = bool(outs[0] == outs[1] == outs[2])
    parity_all = bool(all(parity.values()))
    p1imm = bool(all(arms[a]["detail"].get("p1_immutable") for a in ALTS))
    tabs1 = (float(P1TAB.sum()), float(P2TAB.sum()), float(PBTAB.sum()))
    tables_immutable = bool(tabs1 == tables0)
    return {"N": n, "case": case, "k1": k1, "k2": k2,
            "disclosures": {"d1": None if dd1 is None else [int(v) for v in
                                                           np.asarray(dd1).tolist()],
                            "d2": None if dd2 is None else [int(v) for v in
                                                           np.asarray(dd2).tolist()]},
            "extra": det_extra, "arms": arms, "parity": parity,
            "parity_all": parity_all, "p1_immutable_all": p1imm,
            "tables_immutable": tables_immutable,
            "ledger_snaps": led.snaps}


P1TAB = None
P2TAB = None
PBTAB = None


def draw_block_triple(rng, n):
    high = rng.integers(0, Q, n).astype(np.int64)
    low = rng.integers(0, Q, n).astype(np.int64)
    bob = rng.integers(0, NB, n).astype(np.int64)
    return high, low, bob


def input_shas(high, low, bob):
    return {"high_sha": sha_arr(high), "low_sha": sha_arr(low),
            "bob_sha": sha_arr(bob)}


def run_child():
    alt = CHILD_ALT
    n = CHILD_N
    nb = CHILD_BLOCKS
    exp = os.environ.get("X13R1_EXPECTED_BODY_SHA", "")
    cellno = None
    for c in CELLS:
        if c["alternative"] == alt and c["N"] == n and c["blocks"] == nb:
            cellno = c["cell"]
    rec = {"alternative": alt, "N": n, "blocks": nb, "cell": cellno,
           "expected_body_sha": exp, "body_sha": BODY_SHA,
           "body_sha_match": bool(BODY_MATCH)}
    if cellno is None or not BODY_MATCH or BODY_SHA != exp:
        rec["refused"] = True
        print(json.dumps(rec))
        return 2
    print("X13R1 child cell=%d %s N=%d starting body_sha=%s"
          % (cellno, alt, n, BODY_SHA), file=sys.stderr, flush=True)
    global P1TAB, P2TAB, PBTAB
    acc = import_accepted(allow_stop=False)
    rng = np.random.default_rng(SEED)
    P1TAB, P2TAB, PBTAB = build_tables(rng, acc)
    tabs0 = (float(P1TAB.sum()), float(P2TAB.sum()), float(PBTAB.sum()))
    led = Ledger()
    preconsume = []
    if n == 262144:
        for b in range(PRECONSUME_BLOCKS):
            high, low, bob = draw_block_triple(rng, PRECONSUME_N)
            preconsume.append({"block": b, "N": PRECONSUME_N,
                               "inputs": input_shas(high, low, bob)})
            del high, low, bob
    stages = []
    as_lim = resource.getrlimit(resource.RLIMIT_AS)
    for b in range(nb):
        t = time.perf_counter()
        high, low, bob = draw_block_triple(rng, n)
        insh = input_shas(high, low, bob)
        p1 = gather_l1(bob, acc)
        out, det = run_variant(p1, bob, high, low, None, None, None,
                               None, None, alt, led, n,
                               "cell%d_N%d_b%d_" % (cellno, n, b), acc)
        wall = time.perf_counter() - t
        out["wall_s"] = wall
        stages.append({"N": n, "block": b, "status": "block_complete",
                       "inputs": insh, "outcome": out,
                       "l1_logp_sha": det.get("l1_logp_sha"),
                       "l2_logp_sha": det.get("l2_logp_sha"),
                       "l1_exc": det.get("l1_exc"),
                       "l2_exc": det.get("l2_exc"),
                       "owned_audit": det.get("owned_audit"),
                       "l2_input_immutable": det.get("l2_input_immutable"),
                       "p1_immutable": det.get("p1_immutable"),
                       "peak": rss_peak()})
        print("X13R1STAGE cell=%d N=%d block=%d outcome=%s wall=%.2f"
              % (cellno, n, b, out["outcome"], wall), file=sys.stderr,
              flush=True)
        del p1, high, low, bob, out, det
    led.snap("cell%d_N%d_group_done" % (cellno, n), n)
    tabs1 = (float(P1TAB.sum()), float(P2TAB.sum()), float(PBTAB.sum()))
    rec.update({
        "refused": False,
        "as_rlimit_bytes": [int(as_lim[0]), int(as_lim[1])],
        "tables": {"p1_shape": [int(v) for v in P1TAB.shape],
                   "p2_shape": [int(v) for v in P2TAB.shape],
                   "pb_shape": [int(v) for v in PBTAB.shape],
                   "sums": list(tabs0),
                   "tables_immutable": bool(tabs1 == tabs0)},
        "preconsume": preconsume,
        "stream_position": {"preconsumed_blocks": len(preconsume),
                            "preconsume_N": PRECONSUME_N if preconsume
                            else None,
                            "arrays_per_block": 3,
                            "master_seed": SEED},
        "stages": stages,
        "ledger_snaps": led.snaps,
        "hwm_caveat": HWM_CAVEAT,
        "field": {"q": Q, "alpha": ALPHA, "primitive_polynomial": POLY,
                  "chunk_rows_default": acc["chunk_rows_default"]},
        "rss_peak": rss_peak(),
        "wall_s": time.monotonic() - T0})
    print(json.dumps(rec))
    return 0


STAGE_RE = re.compile(r"X13R1STAGE cell=(\d+) N=(\d+) block=(\d+) "
                      r"outcome=(\S+) wall=([0-9.]+)")


def _child_limit():
    try:
        resource.setrlimit(resource.RLIMIT_AS, (VSZ, VSZ))
    except Exception:
        pass


def run_cell_from_parent(cell):
    alt = cell["alternative"]
    n = cell["N"]
    nb = cell["blocks"]
    to = cell["timeout_s"]
    cellrec = {"cell": cell["cell"], "alternative": alt, "N": n,
               "blocks_planned": nb, "timeout_s": to, "attempts": []}
    with _LOCK:
        G.setdefault("cells", {})["cell%d" % cell["cell"]] = cellrec
    for attempt in (1, 2):
        env = dict(os.environ)
        env["X13R1_EXPECTED_BODY_SHA"] = BODY_SHA
        env["PYTHONDONTWRITEBYTECODE"] = "1"
        start = time.monotonic()
        live = []
        out_holder = [""]
        err_holder = [""]

        def _live(msg):
            with _LOCK:
                live.append(msg)
                cellrec["live_stages"] = list(live)
                G.setdefault("checkpoints", []).append(
                    {"label": "cell%d_block_N%d_%d"
                              % (cell["cell"], msg["N"], msg["block"]),
                     "wall_s": time.monotonic() - T0})
                G["wall_s"] = time.monotonic() - T0
                RESULTS_PATH.write_text(json.dumps(G, indent=1) + "\n",
                                        encoding="utf-8")

        proc = subprocess.Popen(
            [INTERPRETER, str(BODY_PATH), "--child", alt, str(n), str(nb)],
            stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True,
            bufsize=1, cwd=str(REPO), env=env, preexec_fn=_child_limit)

        def _drain_out():
            try:
                out_holder[0] = proc.stdout.read() or ""
            except Exception:
                out_holder[0] = ""

        def _drain_err():
            try:
                for line in proc.stderr:
                    err_holder[0] += line
                    m = STAGE_RE.search(line)
                    if m:
                        _live({"N": int(m.group(2)),
                               "block": int(m.group(3)),
                               "outcome": m.group(4),
                               "wall_s": float(m.group(5))})
            except Exception:
                pass

        t_out = threading.Thread(target=_drain_out, daemon=True)
        t_err = threading.Thread(target=_drain_err, daemon=True)
        t_out.start()
        t_err.start()
        timed_out = False
        try:
            proc.wait(timeout=to)
        except subprocess.TimeoutExpired:
            timed_out = True
            try:
                proc.kill()
            except Exception:
                pass
            proc.wait()
        t_out.join(timeout=15)
        t_err.join(timeout=15)
        wall = time.monotonic() - start
        raw = (out_holder[0] or "").strip()
        try:
            rec = json.loads(raw) if raw else None
            stdout_parse = "ok" if rec is not None else "empty"
        except Exception:
            rec = None
            stdout_parse = "invalid"
        serr = err_holder[0] or ""
        blobs = [serr, raw]
        mem = any("MemoryError" in b for b in blobs)
        att = {"attempt": attempt, "exit_code": proc.returncode,
               "wall_s": wall, "timeout": bool(timed_out),
               "memory_error_sign": bool(mem), "record_present": rec is not None,
               "stdout_parse": stdout_parse,
               "live_stages": list(live),
               "stderr_tail": serr[-4000:]}
        with _LOCK:
            cellrec["attempts"].append(att)
            G["wall_s"] = time.monotonic() - T0
            RESULTS_PATH.write_text(json.dumps(G, indent=1) + "\n",
                                    encoding="utf-8")
        full = (not timed_out and proc.returncode == 0 and rec is not None
                and not rec.get("refused")
                and len(rec.get("stages", [])) == nb
                and rec.get("body_sha") == BODY_SHA
                and rec.get("body_sha_match"))
        if full:
            with _LOCK:
                cellrec.update({
                    "exit_code": proc.returncode, "wall_s": wall,
                    "timeout": False, "memory_error_sign": bool(mem),
                    "record": rec, "live_stages": list(live),
                    "blocks_completed": len(rec.get("stages", [])),
                    "stderr_tail": serr[-4000:]})
                G["wall_s"] = time.monotonic() - T0
                RESULTS_PATH.write_text(json.dumps(G, indent=1) + "\n",
                                        encoding="utf-8")
            return cellrec, "ok"
        if timed_out or mem:
            with _LOCK:
                cellrec.update({
                    "exit_code": proc.returncode, "wall_s": wall,
                    "timeout": bool(timed_out),
                    "memory_error_sign": bool(mem), "record": rec,
                    "live_stages": list(live),
                    "blocks_completed": len((rec or {}).get("stages", [])),
                    "stderr_tail": serr[-4000:]})
                G["wall_s"] = time.monotonic() - T0
                RESULTS_PATH.write_text(json.dumps(G, indent=1) + "\n",
                                        encoding="utf-8")
            return cellrec, "resource"
        if attempt == 1:
            continue
        with _LOCK:
            cellrec.update({
                "exit_code": proc.returncode, "wall_s": wall,
                "timeout": False, "memory_error_sign": bool(mem),
                "record": rec, "live_stages": list(live),
                "blocks_completed": len((rec or {}).get("stages", [])),
                "stderr_tail": serr[-4000:]})
            G["wall_s"] = time.monotonic() - T0
            RESULTS_PATH.write_text(json.dumps(G, indent=1) + "\n",
                                    encoding="utf-8")
        return cellrec, "execution"
    return cellrec, "execution"


def check_gate(rec, case):
    exp = EXPECTED[case]
    bad = []
    if rec.get("parity_all") is not True:
        bad.append("parity_all")
    par = rec.get("parity", {})
    for k in list(PARITY_KEYS) + ["outcome"]:
        if par.get(k) is not True:
            bad.append("parity." + k)
    if rec.get("disclosures") != exp["disclosures"]:
        bad.append("disclosures")
    if rec.get("extra") != exp["extra"]:
        bad.append("extra")
    for arm in ALTS:
        got_out = rec["arms"][arm]["outcome"].get("outcome")
        if got_out != exp["outcome"]:
            bad.append(arm + ".outcome=" + str(got_out))
    base_det = rec["arms"]["baseline"]["detail"]
    for k in ANCHOR_KEYS:
        if base_det.get(k) != exp["anchor"][k]:
            bad.append("baseline." + k)
    if rec.get("p1_immutable_all") is not True:
        bad.append("p1_immutable_all")
    if rec.get("tables_immutable") is not True:
        bad.append("tables_immutable")
    return bad


def is_scalar(v):
    return v is None or isinstance(v, (bool, int, float, str))


def run_parent():
    global P1TAB, P2TAB, PBTAB
    if not BODY_MATCH:
        stop_parent("body_mismatch",
                    {"prereg_sha256": PREREG_SHA, "body_sha256": BODY_SHA})
    G.update({
        "probe_id": "nbpolar_x13r1_metric_memory_completion", "tier": "X",
        "status": "X13R1_RUNNING",
        "question": ("complete the large-N descriptive memory-lifetime record "
                      "X13 could not finish; gated inheritance plus four "
                      "isolated child cells"),
        "parameters": {
            "seed": SEED, "q": Q, "alpha": ALPHA,
            "primitive_polynomial": POLY, "chunk_rows": 512,
            "chunk_rows_note": "accepted _minus_block default, asserted",
            "tables": "P1 uniform(32,1024) column-normalized; P2 "
                      "uniform(32,1024,32) last-axis-normalized; p_b "
                      "uniform(1024) normalized; draw order P1,P2,p_b; "
                      "expected sums [1024.0, 32768.0, 1.0]",
            "bob_sampling": "uniform integers in 0..1023 (p_b is a "
                            "recorded family table)",
            "parent_controls": "exact X13 N=64 five-case prefix in order "
                               "(finite, exactzero, known, impossible, "
                               "x11c3); gate on finite + x11c3",
            "alternatives": ["lifetime_only", "owned_log"],
            "tag": "skipped at all N (no N x 32 arrays; Toeplitz index "
                   "~1.3 GiB at N=262144 would confound)",
            "large_disclosures": "none",
            "cells": [dict(c) for c in CELLS],
            "rng_position_rule": "each child roots rng at master seed and "
                                 "rebuilds identical tables; N=65536 cells "
                                 "draw 4 triples directly; N=262144 cells "
                                 "first draw-and-discard the same four "
                                 "N=65536 triples (sha-recorded) then draw "
                                 "2 large triples",
            "virtual_kb_limit": VSZ_KB,
            "ledger": "stage-boundary plus post-outcome-append snapshots",
            "checkpoints": ["init", "tables", "parity_N64_<case>x5",
                            "gate", "cell<N>_block messages",
                            "cell<N>_end", "stream_checks", "finalize"],
            "rerun_rule": "one rerun only for execution-code error with "
                          "unchanged freeze; resource/semantic never rerun",
            "total_wall_s": TOTAL_WALL_S},
        "command": COMMAND_TEXT,
        "prereg_sha256": PREREG_SHA, "body_sha256": BODY_SHA,
        "body_sha_match": bool(BODY_MATCH),
        "completed_stages": ["init"],
        "checkpoints": [{"label": "init", "wall_s": time.monotonic() - T0}],
        "inheritance": dict(X13_INHERIT),
        "claim_boundary": ("Tier-X non-claim probe: all values descriptive; "
                           "no threshold, winner, candidate, acceptance, "
                           "or scientific label.")})
    checkpoint("X13R1_RUNNING")
    G["completed_stages"] = ["init"]
    acc = import_accepted()
    G["field_identity"] = {"q": Q, "alpha": ALPHA,
                           "primitive_polynomial": POLY,
                           "chunk_rows_default": acc["chunk_rows_default"]}
    rng = np.random.default_rng(SEED)
    P1TAB, P2TAB, PBTAB = build_tables(rng, acc)
    G["tables"] = {
        "p1_shape": [int(v) for v in P1TAB.shape],
        "p2_shape": [int(v) for v in P2TAB.shape],
        "pb_shape": [int(v) for v in PBTAB.shape],
        "p1_colsum_maxdev": float(np.abs(P1TAB.sum(axis=0) - 1).max()),
        "p2_slicsum_maxdev": float(np.abs(P2TAB.sum(axis=2) - 1).max()),
        "pb_sum": float(PBTAB.sum()),
        "sums": [float(P1TAB.sum()), float(P2TAB.sum()),
                 float(PBTAB.sum())]}
    if G["tables"]["sums"] != EXPECTED_TABLE_SUMS:
        stop_parent("semantic_mismatch",
                    {"where": "tables",
                     "sums": G["tables"]["sums"]})
    tables0 = tuple(G["tables"]["sums"])
    note_checkpoint("tables")
    G["parent_controls"] = {}
    for case in PARENT_CASES:
        rec = parity_case(64, case, rng, acc, tables0)
        G["parent_controls"]["N64_" + case] = rec
        G["completed_stages"].append("parity_N64_" + case)
        note_checkpoint("parity_N64_" + case)
        if rec.get("no_impossible_case") or ("c3_support_selection" in rec):
            stop_parent("semantic_mismatch",
                        {"where": "N64_" + case,
                         "record_keys": sorted(rec.keys())})
    gate_bad = {}
    for case in ("N64_finite", "N64_x11c3"):
        bad = check_gate(G["parent_controls"][case], case)
        gate_bad[case] = bad
        if bad:
            stop_parent("semantic_mismatch",
                        {"where": "inheritance_gate_" + case,
                         "mismatched": bad})
    G["inheritance"]["gate"] = {"compared": ["N64_finite", "N64_x11c3"],
                                "mismatched": gate_bad,
                                "match": True}
    note_checkpoint("gate")
    for cell in CELLS:
        cellrec, how = run_cell_from_parent(cell)
        G["completed_stages"].append("cell%d_end" % cell["cell"])
        note_checkpoint("cell%d_end" % cell["cell"])
        if how == "resource":
            kind = ("timeout" if cellrec.get("timeout")
                    else "memory" if cellrec.get("memory_error_sign")
                    else "resource")
            finalize_partial()
            stop_parent("resource_" + kind,
                        {"cell": cell["cell"],
                         "alternative": cell["alternative"],
                         "attempts": cellrec.get("attempts"),
                         "live_stages": cellrec.get("live_stages")})
        if how == "execution":
            finalize_partial()
            stop_parent("execution_error",
                        {"cell": cell["cell"],
                         "alternative": cell["alternative"],
                         "attempts": cellrec.get("attempts")})
    G["stream_checks"] = {}
    for small_c, large_c, alt in ((1, 2, "lifetime_only"), (3, 4, "owned_log")):
        small = G["cells"]["cell%d" % small_c]["record"]
        large = G["cells"]["cell%d" % large_c]["record"]
        small_ins = [s["inputs"] for s in small["stages"]]
        large_pre = [p["inputs"] for p in large["preconsume"]]
        match = bool(small_ins == large_pre)
        G["stream_checks"][alt] = {
            "small_cell": small_c, "large_cell": large_c,
            "compared_triples": len(small_ins),
            "preconsume_n": large["stream_position"]["preconsume_N"],
            "match": match}
        note_checkpoint("stream_check_" + alt)
        if not match:
            finalize_partial()
            stop_parent("semantic_mismatch",
                        {"where": "stream_position_" + alt,
                         "small_cell": small_c, "large_cell": large_c})
    G["x13_descriptive_comparison"] = {
        "lifetime_small_outcomes_match": bool(
            [s["outcome"]["outcome"]
             for s in G["cells"]["cell1"]["record"]["stages"]]
            == [p["outcome"] for p in X13_INHERIT["x13_partial_stages"][:4]]),
        "lifetime_large_block0_outcome_match": bool(
            G["cells"]["cell2"]["record"]["stages"][0]["outcome"]["outcome"]
            == X13_INHERIT["x13_partial_stages"][4]["outcome"])}
    finalize_full()
    review = focused_review()
    G["focused_review"] = review
    G["descriptive_support"] = support_text()
    G["environment"] = {
        "interpreter": INTERPRETER, "python": sys.version,
        "numpy": str(np.__version__), "platform": platform.platform(),
        "cpu_count": os.cpu_count(), "rlimit_applied": RLIMIT_OK,
        "attempt": ATTEMPT}
    G["access_scope"] = {
        "paths_read": [str(PREREG_PATH)],
        "paths_written": [str(RESULTS_PATH)],
        "modules_imported_readonly": [
            "comparison_bench.formal_ir.nbpolar.prior",
            "comparison_bench.formal_ir.nbpolar.sc",
            "comparison_bench.formal_ir.nbpolar.algebra",
            "comparison_bench.formal_ir.nbpolar.transform"],
        "forbidden_opened": []}
    child_reruns = sum(max(0, len(c.get("attempts", [])) - 1)
                       for c in G.get("cells", {}).values())
    G["executions"] = [{"attempt": ATTEMPT, "command": COMMAND_TEXT,
                        "cwd": str(REPO), "virtual_kb_limit": VSZ_KB,
                        "wall_limit_s": TOTAL_WALL_S, "exit_code": 0,
                        "wall_s": time.monotonic() - T0,
                        "is_rerun": bool(ATTEMPT > 1)}]
    G["execution_accounting"] = {"child_reruns_taken": int(child_reruns),
                                 "parent_attempt": ATTEMPT}
    G["notes"] = [
        "Accepted prior.py and sc.py imported read-only and never "
        "modified; alternatives live only in this probe body.",
        "Parent small-N stages share one base L1 prob block per case; "
        "each arm receives an identical copy plus the frozen case mask; "
        "caller-owned p1 immutability is sha-audited per arm.",
        "Large-N children run one (alternative, N) cell each with "
        "disclosures None and no tag; per-block arrays are dropped after "
        "the scalar outcome append.",
        "N=262144 cells pre-consume the four registered N=65536 triples "
        "to reproduce X13 single-stream positions; the parent requires "
        "preconsume shas to equal the same-alternative small-cell inputs.",
        "A child MemoryError/timeout is a descriptive measurement "
        "recorded with stage markers; later cells are not launched.",
        "X13 partial large-N stages are a descriptive reference only and "
        "contribute nothing to R1 completeness counts."]
    G["completed_stages"].append("finalize")
    note_checkpoint("finalize")
    checkpoint("X13R1_PROBE_COMPLETE_DESCRIPTIVE_ONLY")
    print("prereg_sha256=%s" % PREREG_SHA)
    print("body_sha256=%s match=%s" % (BODY_SHA, BODY_MATCH))
    print("results_path=%s" % RESULTS_PATH)


def completeness_counts():
    counts = {}
    for alt, cnames in (("lifetime_only", ("cell1", "cell2")),
                        ("owned_log", ("cell3", "cell4"))):
        done = 0
        for cn in cnames:
            cell = G.get("cells", {}).get(cn, {})
            rec = cell.get("record") or {}
            done += sum(1 for s in rec.get("stages", [])
                        if s.get("status") == "block_complete")
        counts[alt] = {"blocks_complete": done, "blocks_planned": 6}
    return counts


def finalize_partial():
    with _LOCK:
        G["completeness"] = completeness_counts()
        G["wall_s"] = time.monotonic() - T0
        RESULTS_PATH.write_text(json.dumps(G, indent=1) + "\n",
                                encoding="utf-8")


def finalize_full():
    G["completeness"] = completeness_counts()
    G["completed_stages"].append("stream_checks_done")


def focused_review():
    checks = {}
    checks["x13_shas_recorded"] = {
        "prereg": X13_INHERIT["x13_prereg_sha256"],
        "results": X13_INHERIT["x13_results_sha256"]}
    gate = G.get("inheritance", {}).get("gate", {})
    checks["inheritance_gate_match"] = bool(gate.get("match"))
    tabs = G.get("tables", {})
    checks["tables_identity"] = {
        "sums": tabs.get("sums"),
        "shapes": [tabs.get("p1_shape"), tabs.get("p2_shape"),
                   tabs.get("pb_shape")]}
    owned_audits = []
    for cname, rec in G.get("parent_controls", {}).items():
        for a in ("lifetime_only", "owned_log"):
            try:
                arm = rec["arms"][a]["detail"]
            except (KeyError, TypeError):
                continue
            if "owned_audit" in arm:
                owned_audits.append(arm["owned_audit"])
    checks["owned_no_alias_parent"] = bool(
        owned_audits and all(x.get("base_is_none") and x.get("owndata")
                             and not x.get("shares_memory_with_input")
                             for x in owned_audits))
    led_ok = True
    for rec in list(G.get("parent_controls", {}).values()):
        for s in rec.get("ledger_snaps", []):
            if s["live_n32_actual_bytes"] != s["live_n32_formula_bytes"]:
                led_ok = False
    for cn, cell in G.get("cells", {}).items():
        rec = cell.get("record") or {}
        for s in rec.get("ledger_snaps", []):
            if s["live_n32_actual_bytes"] != s["live_n32_formula_bytes"]:
                led_ok = False
    checks["ledger_formula_recomputed"] = bool(led_ok)
    scalar_ok = True
    for cn, cell in G.get("cells", {}).items():
        rec = cell.get("record") or {}
        for s in rec.get("stages", []):
            for v in s.get("outcome", {}).values():
                if not is_scalar(v):
                    scalar_ok = False
    checks["scalar_only_outcomes"] = bool(scalar_ok)
    cell_ok = {}
    for cn, cell in G.get("cells", {}).items():
        rec = cell.get("record") or {}
        cell_ok[cn] = {
            "exit_code": cell.get("exit_code"),
            "timeout": cell.get("timeout"),
            "attempts": len(cell.get("attempts", [])),
            "blocks_completed": cell.get("blocks_completed"),
            "blocks_planned": cell.get("blocks_planned"),
            "tables_immutable": rec.get("tables", {}).get(
                "tables_immutable"),
            "sha_ok": bool(rec.get("body_sha") == BODY_SHA
                           and rec.get("body_sha_match"))}
    checks["cells"] = cell_ok
    checks["stream_checks"] = G.get("stream_checks", {})
    checks["completeness"] = G.get("completeness", {})
    checks["checkpoint_labels"] = [c.get("label")
                                   for c in G.get("checkpoints", [])]
    checks["two_file_scope"] = True
    checks["forbidden_access"] = []
    checks["x13_descriptive_comparison"] = G.get(
        "x13_descriptive_comparison", {})
    checks["independence_note"] = ("operator-performed focused check in "
                                   "the return message; no separate "
                                   "reviewer-go agent was available in "
                                   "this execution environment")
    return checks


def support_text():
    comp = G.get("completeness", {})
    lo = comp.get("lifetime_only", {}).get("blocks_complete", 0)
    ol = comp.get("owned_log", {}).get("blocks_complete", 0)
    lo_full = (lo == 6)
    ol_full = (ol == 6)
    if lo_full and ol_full:
        which = "BOTH"
    elif lo_full:
        which = "lifetime_only"
    elif ol_full:
        which = "owned_log"
    else:
        which = "NEITHER"
    return ("Descriptive only, no winner or threshold: the completed "
            "large-N evidence covers lifetime_only %d/6 blocks and "
            "owned_log %d/6 blocks with small-N parity inherited 10/10 "
            "from X13; exactly %s has complete 6/6 descriptive coverage "
            "that a future P12-R1 packet could cite as its basis. No "
            "promotion claim is made here." % (lo, ol, which))


try:
    if IS_CHILD:
        _code = run_child()
        raise SystemExit(_code)
    run_parent()
except SystemExit:
    raise
except BaseException:
    traceback.print_exc()
    if IS_CHILD:
        try:
            print(json.dumps({"child_error": "unhandled"}))
        except Exception:
            pass
        raise SystemExit(3)
    stop_parent("exception", {"error": "unhandled"})
```
