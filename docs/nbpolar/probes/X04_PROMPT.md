Execute `docs/nbpolar/probes/X04_L1_DISCLOSURE_PARETO.md` exactly as a Tier-X
probe. Freeze the complete executable body before any decoder call; reuse the
accepted P5/X03b machinery without production edits; write only the two
specified probe files; and obtain one independent reviewer-go focused review.
Return the per-K1 L1/end-to-end/oracle counts, paired tables, first-recovery and
regression counts, disclosure and planning-only f curve, integrity/resource
accounting, and review verdict. Do not create a threshold, scientific status,
attempt, candidate/accepted claim, ledger update, commit or push.
