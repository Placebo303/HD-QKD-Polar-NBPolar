# X03b copy-paste prompt

Execute `X03B_ORACLE_LABEL_CORRECTION.md` as a Tier-X delta successor. Inherit
X03 verbatim except for the one registered oracle final-label correction.
Freeze the full executed body before decoding, write only the two registered
files, include per-block scalar records, and obtain one independent reviewer-go
focused review. Return the three paired tables and gap-closure result without
pass/fail, candidate/accepted, production edits, ledger updates, commit/push.
