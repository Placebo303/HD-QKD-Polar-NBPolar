# X02 copy-paste prompt

Execute `docs/nbpolar/probes/X02_DEPENDENT_L2_POINT_SEARCH.md` as a Tier-X
non-claim probe. Write the required three-section preregistration before any
decoder call, then execute exactly the frozen 60 configurations. Keep only
`prereg.md` and `results.json` in the registered probe root. Perform a focused
read-only arithmetic/completeness/truth-isolation review and return the ranked
summary plus any configurations in the descriptive region. Do not widen the
grid, create a decision token, update milestone ledgers, commit or push.
