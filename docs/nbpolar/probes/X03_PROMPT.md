# X03 copy-paste prompt

Execute `docs/nbpolar/probes/X03_BOB_MARGINAL_L2.md` as Tier X. Freeze the
preregistration before any decoder call, run all five seeds on common blocks,
keep exactly two output files, and perform one focused independent review.
Return the three-arm summary and gap-closure fraction without pass/fail,
candidate/accepted, production edits, ledger updates, commit or push.
